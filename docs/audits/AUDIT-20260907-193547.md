# Deep adversarial audit — anilist-recommender

**Timestamp**: 20260907-193547
**Commit**: b9cd2a6 (working tree pulita)
**Ruoli attivi**: security (2 partizioni), logic (3), logic+contract (1), logic+performance (1), contract (1), test-quality (2), dependency (1), + UI roles (2), 13 partizioni totali, 13 attacker + 14 verifier + 1 mapper = 42 agenti

## Executive summary

| Severità | N. findings |
|---|---|
| CRITICAL | 0 |
| HIGH | 5 |
| MEDIUM | 16 |
| LOW | 56 |
| INFO | 4 |
| **Totale validi** | 81 |
| Scartati (falsi positivi) | 18 |

**Aree più a rischio**: src/server/setup.ts (12), src/server/recommend.ts (8), src/server/anilist.ts (6), tests/scoring.test.ts (6), src/ui/App.tsx (5)

Nessun CRITICAL. Il tema dominante: **validazione Host/Origin assente sulla superficie HTTP locale** (bind 127.0.0.1 è l'unica barriera; DNS rebinding da browser aggira il loopback), **profilo gusti inserito in ordine di inserimento Map invece che per affinità** (degrada silenziosamente i consigli sui profili grandi), **wizard setup disaccoppiato dal job di download reale** (schermata progresso mai raggiunta, UI "starting" durante un download da GB), **form bloccato dopo la prima ricerca riuscita**. Su 81 findings validi, 56 sono LOW (debito tecnico reale ma non manifestabile oggi).

## Metodologia

- **Mappatura**: TypeScript (Node ≥22, Hono + React/Vite), ~3.655 LOC, isML: no (LLM solo via HTTP)
- **Flotta**: 13 attacker su 13 partizioni, 14 verifier (batch per file, sempre ≠ attacker)
- **Scope coperto**: tutto src/server, src/shared, src/ui, tests/, scripts/, .github/, package.json, pnpm-lock.yaml
- **Non coperto**: node_modules, dist, docs, fixtures (dati), .env.example
- **Verifica**: ogni finding confermato da almeno un verifier indipendente; severità assegnata dal verifier secondo la rubrica. Mutation testing empirico usato dai verifier per diverse claims (test-quality e logic).
- **Nota operativa**: il verifier di setup.ts è morto per rate-limit API nel run finale; i 12 finding setup.ts sono quelli verificati nel run precedente (tutti CONFIRMED, evidenza nel journal). 5 finding di attacker HTTP-surface persi nel run 1 e rigenerati nel run 2.

## Findings

### HIGH

#### logic-4.1 — Candidate queries use loved dims in insertion order, not by affinity — the strongest tastes never generate candidates
- **File**: `src/server/candidates.ts:4`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/server/candidates.ts)
- **Severità**: HIGH
- **Descrizione/evidenza**: `const top = (p: TasteProfile, dim: string, n: number): string[] => p.loved.filter((d) => d.dim === dim).slice(0, n).map((d) => d.value);` (candidates.ts:4-8) — no sort. `p.loved` is NOT sorted by affinity: profile.ts finalizeSides builds it in Map insertion order and caps with `.slice(0, n)` (src/server/profile.ts:69-79), and scoring.ts itself has to sort locally before using it: `for (const d of [...p.loved].sort((a, b) => b.aff - a.aff))` (src/server/scoring.ts:75). Verified repro: profile with loved genres in first-seen order [Slice of Life aff=0.061, Psychological aff=0.166, Fantasy aff=0…
- **Verifica**: Verificato end-to-end sul codice reale. finalizeSides (profile.ts:58-87) costruisce p.loved in ordine di inserimento della Map e cappeerà con filter+slice, senza sort; con WEIGHTS.topGenres=8 / topTags=20 (config.ts:95-96) top() (candidates.ts:4-8) seleziona 3 generi su 8 e 5 tag su 20 per primo-arrivato, non per affinità. Nessuna mitigazione a monte: recommend.ts:40-46 passa l'output di buildProfile direttamente a fetchCandidates, e nessun test copre la selezione delle query. Repro empirico ese…
- **Fix suggerito**: Sort by affinity before slicing in `top`: `p.loved.filter((d) => d.dim === dim).sort((a, b) => b.aff - a.aff).slice(0, n).map((d) => d.value)` (one-line change, mirrors scoring.ts:75). Optionally sort once in finalizeSides so every consumer gets affinity order.
- **Patch proposta**: [logic-logic-4.1-1.patch](patches/AUDIT-20260907-193547/logic-logic-4.1-1.patch)
- **Rischio residuo se non corretto**: degrado funzionale visibile all'utente o finestra di attacco locale permanente

#### logic+contract-5.1 — finalizeSides caps loved/disliked by Map insertion order, never by affinity — the real 'top' values are silently dropped
- **File**: `src/server/profile.ts:69`
- **Rilevato da**: logic+contract · **Verificato da**: verifier indipendente (batch src/server/profile.ts)
- **Severità**: HIGH
- **Descrizione/evidenza**: `const cap = (dim: Dim, n: number) => loved.filter((d) => d.dim === dim).slice(0, n);` (profile.ts:69-72). `loved` is filled by iterating `acc` in insertion order (profile.ts:61), i.e. the order values are first met while scanning `entries`; `aff` is computed (profile.ts:64) but never used for ranking. WEIGHTS names say otherwise (`topTags`, `topGenres`, `topStudios`, `topDisliked`, config.ts) and other consumers re-sort defensively (scoring.ts:75 `[...p.loved].sort((a, b) => b.aff - a.aff)`, scoring.ts:128), while candidates.ts `top(profile,'genre',3)` (candidates.ts:4-8) and llm.ts `profile.…
- **Verifica**: Riprodotto (node --experimental-strip-types su src/server/profile.ts): 8 generi deboli incontrati per primi (4 entry a 70 ciascuno, aff 0.137) + un genere BEST incontrato per ultimo con 2 entry a 100 (aff 0.200, il massimo) → finalizeSides produce loved nell'ordine di inserimento della Map e cap('genre',8) taglia BEST del tutto; candidates.ts top(profile,'genre',3) interrogarebbe W1,W2,W3. Nessuna mitigazione a monte: fetchUserList (anilist.ts:226-255) preserva l'ordine delle liste API (per stat…
- **Fix suggerito**: Sort before slicing in finalizeSides: `const cap = (dim: Dim, n: number) => loved.filter((d) => d.dim === dim).sort((a, b) => b.aff - a.aff).slice(0, n);` and `const capNeg = (dim: Dim, n: number) => disliked.filter((d) => d.dim === dim).sort((a, b) => a.aff - b.aff).slice(0, n);` (optionally tie-break by `support` desc for stability). No other change needed: scoring.ts already re-sorts, candidate
- **Patch proposta**: [logic+contract-logiccontract-5.1-2.patch](patches/AUDIT-20260907-193547/logic+contract-logiccontract-5.1-2.patch)
- **Rischio residuo se non corretto**: degrado funzionale visibile all'utente o finestra di attacco locale permanente

#### logic+security-10.1 — Wizard step machine is decoupled from the real download: progress screen (step 2) is dead code, user gets 'starting' UI for the whole download
- **File**: `src/ui/components/SetupWizard.tsx:40`
- **Rilevato da**: logic+security · **Verificato da**: verifier indipendente (batch src/ui/components/SetupWizard.tsx)
- **Severità**: HIGH
- **Descrizione/evidenza**: `const step = status.setupDone ? 3 : downloaded ? 2 : 1;` — the 'Download' button (line 121-126) calls `onClick={() => finish({ model })}` -> `postSetup("finish", body)` (line 45). On the server `POST /finish` only marks `setupDone: true` and starts the download in the background via `ensureLlmServer()` (src/server/setup.ts:369-375 -> run() at 258-276, which does `lms get` with a 60-minute timeout), leaving `job.state === "idle"` and `job.logTail === ""`. Consequences: (a) step jumps 1 -> 3 immediately and the user stares at 'Avvio il backend LLM…' with no progress/log for up to the whole down…
- **Verifica**: Verificato end-to-end: il bottone Download (SetupWizard.tsx:123) chiama finish({model}) -> POST /finish (setup.ts:374-382) che setta setupDone:true e lancia ensureLlmServer() in background FUORI dal job singleton (job resta idle, logTail vuoto). postSetup("download") non ha call site in src/ui (grep: solo dichiarazione api.ts:37). Quindi step salta 1->3 subito e la schermata 3 mostra solo testo italic senza progress/log per l'intero download (lms get, timeout 60 min). Step 2 e' irraggiungibile d…
- **Fix suggerito**: Derive the step from server truth instead of `downloaded`: `const step = status.setupDone ? 3 : status.job.state === "downloading" || status.job.state === "installing-cli" ? 2 : downloaded ? 3 : 1;` and make the Download button actually start the job: `await postSetup("download", { model }); await finish({ model });` (or have finish return before the background ensure). Server-side, keep returning
- **Patch proposta**: [logic+security-logicsecurity-10.1-3.patch](patches/AUDIT-20260907-193547/logic+security-logicsecurity-10.1-3.patch)
- **Rischio residuo se non corretto**: degrado funzionale visibile all'utente o finestra di attacco locale permanente

#### logic+security-10.2 — phase never returns to 'idle': the search form is permanently disabled after the first successful search
- **File**: `src/ui/App.tsx:137`
- **Rilevato da**: logic+security · **Verificato da**: verifier indipendente (batch src/ui/App.tsx)
- **Severità**: HIGH
- **Descrizione/evidenza**: `<UsernameForm lang={lang} busy={phase === "profile" || phase === "recos"} onSubmit={run} />` and UsernameForm renders `disabled={!valid || props.busy}` (src/ui/components/UsernameForm.tsx:30). `run()` sets `setPhase("profile")` then, on success, `setResult(r); setPhase("recos")` (lines 65-66) — and 'recos' is the terminal state, never reset. So after the first successful search the submit button stays disabled for the rest of the session (no reload = no second search). Supporting evidence the state machine conflates terminal and in-flight: the loading branch `{phase === "recos" && !result && …
- **Verifica**: Verificato end-to-end su App.tsx + UsernameForm.tsx. busy copre 'profile' e 'recos' (App.tsx:137); l'unico path che torna a 'idle' è il catch (linea 86), il successo termina in setPhase("recos") (linea 66) senza mai resettare. Doppio blocco: button disabled={!valid || props.busy} (UsernameForm.tsx:30) e guard if (valid && !props.busy) nell'onSubmit (riga 16). Nessun altro writer di phase, nessun bottone 'nuova ricerca': dopo il primo successo la form è morta per tutta la sessione. Il ramo loadin…
- **Fix suggerito**: Track in-flight explicitly instead of reusing the terminal phase: `const [loading, setLoading] = useState(false)` set true at the top of `run()`, false in a `finally`, pass `busy={loading}`; keep `phase` only for what to render. Guard the explain upgrade with a request sequence: `const seq = ++runSeq.current; fetchExplain(...).then(ex => { if (seq !== runSeq.current) return; ... })`.
- **Patch proposta**: [logic+security-logicsecurity-10.2-4.patch](patches/AUDIT-20260907-193547/logic+security-logicsecurity-10.2-4.patch)
- **Rischio residuo se non corretto**: degrado funzionale visibile all'utente o finestra di attacco locale permanente

#### security+performance-3.1 — 429 retry loop is unbounded and treats a malformed/zero Retry-After as "no delay", hammering the API forever
- **File**: `src/server/anilist.ts:138`
- **Rilevato da**: security+performance · **Verificato da**: verifier indipendente (batch src/server/anilist.ts)
- **Severità**: HIGH
- **Descrizione/evidenza**: anilist.ts:138-142 — `if (res.status === 429) { const retryAfter = Number(res.headers.get("retry-after") ?? 5); await sleep(Math.min(retryAfter, 60) * 1000); continue; }`. Unlike the 5xx and network-error branches, which both gate on `attempt < 3`, the 429 branch has no attempt cap, so a persistently rate-limited endpoint hangs the request forever (callers' `.catch(() => ...)` in candidates.ts:39 / recommend.ts:83 never fire, the HTTP handler never answers). Secondary edge: if the header is present but non-numeric (HTTP-date form) `Number(...)` is NaN, `Math.min(NaN, 60)` is NaN, `setTimeout(r…
- **Verifica**: Verificato sul codice: il ramo 429 (anilist.ts:138-142) e' l'unico senza gate su `attempt` (network e 5xx gateano su attempt<3 a righe 132/143), quindi 429 persistenti = loop infinito e richiesta mai risolta (nessun timeout server-side in index.ts, i .catch dei caller non coprono fetchUserList). Edge secondario provato empiricamente su Node 22: setTimeout(fn, NaN|Infinity|0|-500) scatta tutti in ~1ms, quindi Retry-After in forma HTTP-date (Number->NaN) o vuoto (Number('')=0) degrada il loop a bu…
- **Fix suggerito**: Cap 429 retries and sanitize the delay: `const raw = Number(res.headers.get("retry-after")); const wait = Number.isFinite(raw) && raw > 0 ? Math.min(raw, 60) : 5; if (attempt >= 10) throw new AniListError("AniList rate-limited", 429); await sleep(wait * 1000); continue;`
- **Patch proposta**: [security+performance-securityperformance-3.1-5.patch](patches/AUDIT-20260907-193547/security+performance-securityperformance-3.1-5.patch)
- **Rischio residuo se non corretto**: degrado funzionale visibile all'utente o finestra di attacco locale permanente

### MEDIUM

#### dependency-13.1 — CI pnpm toolchain unpinned: corepack without packageManager field, while lockfile/build policy require pnpm >=11
- **File**: `.github/workflows/ci.yml:13`
- **Rilevato da**: dependency · **Verificato da**: verifier indipendente (batch .github/workflows/ci.yml)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: .github/workflows/ci.yml:13 runs `- run: corepack enable` followed by `pnpm install --frozen-lockfile` (ci.yml:14), but package.json (lines 1-32) has no `"packageManager"` field. Without it, corepack does not pin pnpm: the shim falls back to its bundled "last known good" pnpm resolved at CI time, decoupled from the version that produced the lockfile. The repo locally was generated with pnpm 12.3.4 (`pnpm --version` on this machine) and pnpm-workspace.yaml:1-2 declares `allowBuilds:\n  esbuild: true`, a setting that only exists since pnpm 11 (it replaced onlyBuiltDependencies, which was removed…
- **Verifica**: Verificato sul codice reale: package.json (1-32) non ha alcun campo packageManager, grep su tutto il repo (escluso lockfile) trov zero occorrenze, nessun .npmrc o pin alternativo; quindi `corepack enable` (ci.yml:13) + `pnpm install --frozen-lockfile` (ci.yml:14) eseguono la pnpm "last known good" risolta dal corepack del runner. Riprodotto empiricamente: `corepack pnpm --version` in dir senza packageManager risolve 12.3.4 dal LKG di corepack 0.34.6. La parte allowBuilds e' confermata da fonte a…
- **Fix suggerito**: Pin the package manager in package.json so corepack installs exactly that version:

```diff
--- a/package.json
+++ b/package.json
@@
   "type": "module",
   "license": "MIT",
+  "packageManager": "pnpm@12.3.4",
   "engines": {
```

Optionally also pin Node in ci.yml to a specific line matching engines (e.g. `node-version: 22.18` or add engines check) so the CI toolchain is fully reproducible. No w
- **Patch proposta**: [dependency-dependency-13.1-6.patch](patches/AUDIT-20260907-193547/dependency-dependency-13.1-6.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### dependency-13.2 — GitHub Actions pinned to mutable major tags instead of commit SHAs
- **File**: `.github/workflows/ci.yml:9`
- **Rilevato da**: dependency · **Verificato da**: verifier indipendente (batch .github/workflows/ci.yml)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: ci.yml:9 `uses: actions/checkout@v4` and ci.yml:10 `uses: actions/setup-node@v4` reference mutable tags. If either upstream tag is moved or the action's repo is compromised, arbitrary code runs in CI with the workflow's GITHUB_TOKEN on every push and pull_request (ci.yml:2-4), which for a self-hosted project is the path to injecting code into the repo's build artifacts. Both are first-party GitHub actions with no secrets in the workflow, so the blast radius is limited to repo token + code execution, not credentials.
- **Verifica**: Confermato dalla lettura integrale del file: ci.yml:9 `uses: actions/checkout@v4` e ci.yml:10 `uses: actions/setup-node@v4` sono tag mobili, con trigger push+pull_request (ci.yml:2-4) che li esegue ad ogni evento. Nessuna mitigazione presente: il workflow non dichiara un blocco `permissions:` (GITHUB_TOKEN ai permessi default del repo, non least-privilege), non ci sono secret nel workflow, e .github/ contiene solo ci.yml (nessun dependabot/renovate a mantenere i pin aggiornati). Il vettore descr…
- **Fix suggerito**: Pin both actions to full commit SHAs, keeping the version as a comment:

```diff
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@
-      - uses: actions/checkout@v4
-      - uses: actions/setup-node@v4
+      - uses: actions/checkout@<full-40-char-sha> # v4.x
+      - uses: actions/setup-node@<full-40-char-sha> # v4.x
```

Resolve the SHAs with `gh api repos/actions/checkout/git/re
- **Patch proposta**: [dependency-dependency-13.2-7.patch](patches/AUDIT-20260907-193547/dependency-dependency-13.2-7.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### logic-2.1 — runInstall has no timeout and no cancel: a hanging install leaves the job permanently busy
- **File**: `src/server/setup.ts:203`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: runInstall() spawns `bash -c "curl -fsSL https://lmstudio.ai/install.sh | bash"` (line 197) but, unlike runLms (which has a setTimeout+kill at lines 120-125), it registers only stdout/stderr/error/close handlers — there is no timer and no cancel path. The busy guard `if (job.state === "downloading" || job.state === "installing-cli") throw new Error("busy")` (lines 170, 188) then makes POST /setup/install-cli and POST /setup/download return 409 forever if the install never exits (blocked network, curl hanging, prompt waiting for input). Only an app restart recovers.
- **Verifica**: Verificato: runInstall (setup.ts:203-214) non ha timer né kill, a differenza di runLms (120-125); i busy guard a 170 e 188 bloccano /install-cli e /download con 409; job è module-level e /reset non lo tocca → solo il restart recupera. install-cli È cablato nella wizard (SetupWizard.tsx:109) e jobBusy disabilita il bottone, quindi un curl|bash appeso (curl senza --max-time, stdin /dev/null) bricka l'onboarding finché l'app non riparte. Trigger raro ma impatto alto sul flusso primario → MEDIUM (no…
- **Fix suggerito**: Add a bounded timer like runLms (e.g. 10 min for the CLI installer), send SIGTERM then SIGKILL after a grace period, and set `job = { state: "error", ... }` on timeout. Optionally a POST /setup/job/cancel that kills the child and resets `job`.
- **Patch proposta**: [security+logic-logic-2.1-8.patch](patches/AUDIT-20260907-193547/security+logic-logic-2.1-8.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### logic-4.2 — MAX_DEPTH=10 truncates prequel chains: one long franchise splits into several rootIds and a dropped prequel beyond depth 10 is missed
- **File**: `src/server/franchise.ts:16`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/server/franchise.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: `const MAX_DEPTH = 10;` (franchise.ts:16) bounds the walk `for (let hop = 0; hop < MAX_DEPTH; hop++)` (franchise.ts:40) and `rootId: chain[0] ?? m.id` (franchise.ts:70). Verified repro: a single 14-entry chain (each entry PREQUEL→previous) yields 4 distinct rootIds ({9001: 11 members, 9002: 1, 9003: 1, 9004: 1}) → dedupeFranchises keeps one representative per root, so the same series gets multiple cards with wrong groupSize. Worse, with listMap {9001: DROPPED}, candidates at depth 13/14 are classified `ENTRY_POINT ENTRY_POINT` instead of EXCLUDED — a user who dropped the root gets the sequel r…
- **Verifica**: Repro sul codice reale (franchise.ts:16,40,70): catena lineare 9001→9014 con ogni entry PREQUEL→precedente dà esattamente 4 rootId {9001:11 membri, 9002:1, 9003:1, 9004:1}; dedupeFranchises (scoring.ts:195-212) tiene un rappresentante per root → card duplicate della stessa serie con groupSize errato (11 su un gruppo, 1 sugli altri). Con listMap {9001:DROPPED} il candidato 9014 risulta ENTRY_POINT/entryPointId 9004 invece di EXCLUDED (droppedId null), quindi badge UI "Start here" (strings.ts:40) …
- **Fix suggerito**: Resolve roots once per chain instead of per candidate: memoize `rootOf(id)` (walk prequel edges with a shared visited set, cache the result per id) and drop the hop cap, or raise MAX_DEPTH and make the root canonical by caching `chain[0]` per id so every member of a chain converges to the same rootId. Dropped/unseen detection then runs over the same memoized full chain.
- **Patch proposta**: [logic-logic-4.2-9.patch](patches/AUDIT-20260907-193547/logic-logic-4.2-9.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### logic-4.3 — Calling dedupeFranchises twice (recommend.ts:101→108) resets every groupSize to 1 — the "{n} more in this series" badge can never render
- **File**: `src/server/recommend.ts:108`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/server/recommend.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: dedupeFranchises recomputes sizes from its own input array: `sizeByRoot.set(r.rootId, (sizeByRoot.get(r.rootId) ?? 0) + 1)` (src/server/scoring.ts:200) and annotates `{ ...r, groupSize: sizeByRoot.get(r.rootId) ?? 1 }` (scoring.ts:210). recommend.ts calls it twice: `const deduped = dedupeFranchises(scored).slice(0, 50);` (recommend.ts:101) then `const withGroups = dedupeFranchises(deduped);` (recommend.ts:108). The second input already holds one entry per root, so every count is 1. Verified repro: two candidates sharing rootId 100 → after 1st dedupe `[[1, 2]]`, after 2nd dedupe `[[1, 1]]`. UI …
- **Verifica**: Verified with the real scoring.ts: dedupeFranchises recomputes sizeByRoot from its own input (scoring.ts:200,210), and recommend.ts calls it twice (101 then 108). Repro with scoreAll+dedupeFranchises: after 1st call groupSize=3 (pool-wide), after 2nd call groupSize=1 for the same reco. Since the 2nd input holds one entry per root, every count is 1, so RecoCard.tsx:46 `r.groupSize > 1` is unreachable and the "{n} more in this series" badge (strings.ts:43/127) is dead in production. Tests cover de…
- **Fix suggerito**: In recommend.ts drop the second call (the relink loop at lines 103-107 only rewrites rootId of already-unique representatives), or carry the size through: `const withGroups = deduped.map(r => r.rootId == null ? r : { ...r, groupSize: firstPassSize.get(r.media.id) ?? r.groupSize })`. Alternatively make dedupeFranchises idempotent by not recomputing when `r.groupSize > 1`.
- **Patch proposta**: [logic-logic-4.3-10.patch](patches/AUDIT-20260907-193547/logic-logic-4.3-10.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### logic-4.4 — All candidate-page fetch failures swallowed silently: an AniList outage/rate-limit presents an empty pool as "no recommendations"
- **File**: `src/server/candidates.ts:39`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/server/candidates.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: `const results = await Promise.all(queries.map((q) => fetchMediaPage(q).catch(() => ({ media: [], hasNextPage: false }))));` — the catch discards AniListError (502 unreachable, GraphQL errors such as an unrecognized tag name, or 429 after retries are exhausted) with no log, no counter, no rethrow. If all ~13 queries fail, `pool` is empty and recommendFor returns `{recos: [], avoided: []}`, which the UI renders as the empty state — indistinguishable from a user with a fully-watched list. Failures are also partial-and-invisible: a bad loved tag silently drops 1 of 5 tag queries.
- **Verifica**: Confermato dal codice: candidates.ts:39 cattura ogni reject e ritorna {media: [], hasNextPage: false} senza log/counter/rethrow. L'errore non propaga, quindi il mapping AniListError→502 di api.ts:69-74 non scatta mai per le pagine candidate: recommendFor ritorna {recos: [], avoided: []} → HTTP 200 → App.tsx:152 renderizza l'emptyState, indistinguibile da una lista completamente vista. Aggravante non citata: getRecommendation (recommend.ts:24-31) cache-izza il risultato vuoto per 10 minuti, quind…
- **Fix suggerito**: Track failures and surface them: `let failed = 0; ... .catch(() => { failed++; return { media: [], hasNextPage: false }; })`, then `if (failed === queries.length) throw new AniListError("candidate fetch failed", 502)` and log a warning for partial failures (or at minimum `console.warn` per rejected query).
- **Patch proposta**: [logic-logic-4.4-11.patch](patches/AUDIT-20260907-193547/logic-logic-4.4-11.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### logic+security-10.3 — finish() has no catch: setup action failures are invisible (unhandled rejection, buttons just re-enable)
- **File**: `src/ui/components/SetupWizard.tsx:42`
- **Rilevato da**: logic+security · **Verificato da**: verifier indipendente (batch src/ui/components/SetupWizard.tsx)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: `async function finish(body) { setBusy(true); try { await postSetup("finish", body); const s = await fetchSetupStatus(); setStatus(s); } finally { setBusy(false); } }` — there is no `catch`, so any rejection (network error, 400/409 from the server) becomes an unhandled promise rejection with zero UI feedback; the user just sees the button re-enable as if nothing happened. Concrete case: the custom URL check is only `!/^https?:\/\//.test(customUrl)` (line 180), so `http://127.0.0.1:11434/v1?api=1` passes client-side but the server rejects it with 400 `invalid_url` (`/^https?:\/\/[\w.:/-]+$/`, s…
- **Verifica**: Codice confermato: finish e' try/finally senza catch; postSetup->json() throwa su !res.ok e i tre onClick={() => finish(…)} scartano la promise -> unhandled rejection senza feedback (nessun handler globale: main.tsx e' solo createRoot, niente unhandledrejection/ErrorBoundary). Caso concreto verificato: '?' non matcha /^https?:\/\/[\w.:/-]+$/ del server (setup.ts:365) ma passa il check client (riga 180) -> 400 invalid_url silenzioso. Stessa classe su install-cli (.catch(()=>undefined)) che ingoia…
- **Fix suggerito**: Add `const [err, setErr] = useState<string|null>(null)`; in `finish` wrap in try/catch: `catch (e) { setErr(String((e as Error).message)); }` and render it next to the actions (reuse the `status.job.state === "error"` block styling). Align the client URL check with the server (or use `new URL(customUrl)` + protocol/host check) so an invalid URL is blocked before submission.
- **Patch proposta**: [logic+security-logicsecurity-10.3-12.patch](patches/AUDIT-20260907-193547/logic+security-logicsecurity-10.3-12.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### logic+security-10.4 — Unvalidated lang from localStorage throws in tr() and white-screens the app on every reload
- **File**: `src/ui/App.tsx:19`
- **Rilevato da**: logic+security · **Verificato da**: verifier indipendente (batch src/ui/App.tsx)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: `const [lang, setLang] = useState<Lang>((localStorage.getItem("lang") as Lang | null) ?? "en");` accepts any stored string. `tr` does `let s = DICTS[lang][key] ?? DICTS.en[key] ?? key;` (src/shared/strings.ts:178) — for any value not exactly "en"/"it" (e.g. "EN", "fr", hand-edited or written by another app on the same origin), `DICTS[lang]` is undefined and the property read throws TypeError during the first render. Because the bad value is persisted (line 40 writes it back on every `lang` change), the crash reproduces on every load until localStorage is manually cleared.
- **Verifica**: Confermato: useState casta Qualsiasi stringa da localStorage (App.tsx:19-21) e tr fa DICTS[lang][key] senza optional chaining (strings.ts:178) — per un valore != 'en'/'it' il property read lanca TypeError durante il primo render. main.tsx monta <App/> senza error boundary → white screen totale. Il valore è persistito (App.tsx:40) e riletto a ogni load, quindi il crash è persistente finché localStorage non viene svuotato a mano. Mitigante sulla frequenza: l'unico writer nel repo è questa app e sc…
- **Fix suggerito**: Validate before casting: `const saved = localStorage.getItem("lang"); const [lang, setLang] = useState<Lang>(saved === "it" ? "it" : "en");` — or make `tr` null-safe: `DICTS[lang]?.[key] ?? DICTS.en?.[key] ?? key`.
- **Patch proposta**: [logic+security-logicsecurity-10.4-13.patch](patches/AUDIT-20260907-193547/logic+security-logicsecurity-10.4-13.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### performance+logic-6.1 — Deterministic fallback explanations are cached for 7 days, permanently masking LLM failures as source:"cache"
- **File**: `src/server/llm.ts:138`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/llm.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: llm.ts:137-140 `// cache fallbacks too so the whole batch is served from cache next time` + `if (!fresh.has(String(r.media.id))) fresh.set(String(r.media.id), r.why);` writes the deterministic `r.why` into the cache with `exp: Date.now() + CACHE_TTL_EXPL_MS` (7 days, config.ts CACHE_TTL_EXPL_MS), and llm.ts:116 serves it as `{ text: hit, source: "cache" }`. The whole LLM call is wrapped in `catch { // LLM unreachable/slow — deterministic fallbacks already in place }` (llm.ts:143-145), so a single transient failure (LLM_TIMEOUT_MS 30s timeout at llm.ts:23, HTTP 5xx at llm.ts:25, or a malformed …
- **Verifica**: Il meccanismo centrale regge: llm.ts:138-140 scrive il why deterministico in cache con CACHE_TTL_EXPL_MS=7d (config.ts:62) e llm.ts:116 lo serve come source:"cache"; nessun path di retry esiste (cache hit → pending 0 → early return a llm.ts:119, l'LLM non è mai richiamato prima della scadenza), e il catch a 143 è muto (nessun log, /api/health continua a riportare enabled:true via llmHealth su /models, api.ts:17). Però l'evidence dell'attacker è sbagliata sui trigger: il catch avvolge TUTTO il lo…
- **Fix suggerito**: Do not cache fallbacks: drop the `for (const r of batch) { if (!fresh.has(...)) fresh.set(...) }` loop (llm.ts:138-140) so only LLM-produced text is persisted; the deterministic why is already the in-memory fallback at llm.ts:116. If cache-warmth for fallbacks is wanted, store a marker (e.g. `{exp: Date.now()+5*60_000, items, fallback:true}`) or give fallback entries a short TTL and a distinct sou
- **Patch proposta**: [performance+logic-performancelogic-6.1-14.patch](patches/AUDIT-20260907-193547/performance+logic-performancelogic-6.1-14.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### performance+logic-6.2 — parseExplanations greedy bracket slice loses the entire batch on prose containing ']' or truncated output (no max_tokens set)
- **File**: `src/server/llm.ts:64`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/llm.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: `const start = raw.indexOf("["); const end = raw.lastIndexOf("]"); ... JSON.parse(raw.slice(start, end + 1))` — verified empirically against the shipped function: input `"Sure!\n```json\n[{"id":1,"why":"a"}]\n```\nNote: items marked [x] are skipped."` -> `[]`, and a length-truncated reply `"[{"id":1,"why":"a"},{"id":2,"wh"` -> `[]` (no closing bracket => lastIndexOf = -1). One bad batch silently discards all 10 explanations (catch at llm.ts:72-74 returns []) and, combined with performance+logic-6.1, those 10 items then serve deterministic text for 7 days. The request at llm.ts:22 sets only `{ …
- **Verifica**: Riprodotto empiricamente con copia verbatim della funzione: input con fence JSON + prose finale contenente '[x]' → [] (lastIndexOf(']') cade oltre la fine dell'array, JSON.parse fallisce, catch→[]); input troncato senza ']' → end=-1, guard `end <= start` → []. Verificato anche che il caso meno severo (parentesi bilanciate dentro la stringa why) funziona, quindi il difetto è ristretto a ']' post-array e a output troncato — non 'qualsiasi bracket nel prose'. La catena di impatto regge: batch inter…
- **Fix suggerito**: Replace the two-index slice with a quote-aware depth scan from the first `[` to its matching `]` (bail out at the first balanced array instead of the last `]`), and add `max_tokens` to the request body plus a check of `json.choices[0].finish_reason === "length"` to log/skip caching on truncation. Optionally send `response_format: { type: "json_object" }` (wrapping the array in an object) which mos
- **Patch proposta**: [performance+logic-performancelogic-6.2-15.patch](patches/AUDIT-20260907-193547/performance+logic-performancelogic-6.2-15.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### security-1.1 — Endpoint di setup privilegiati esposti senza autenticazione: install-cli, download, reset, finish
- **File**: `src/server/api.ts:23`
- **Rilevato da**: security · **Verificato da**: verifier indipendente (batch src/server/api.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: src/server/api.ts:12-23 monta la superficie senza alcun middleware di auth: `export const api = new Hono();` … `api.route("/setup", setupRoutes);`. Le route montate eseguono azioni privilegiate con un solo POST non autenticato: src/server/setup.ts:334-341 `setupRoutes.post("/install-cli")` → setup.ts:193/197 esegue `irm https://lmstudio.ai/install.ps1 | iex` (Windows) e `curl -fsSL https://lmstudio.ai/install.sh | bash` (mac) come utente del processo; setup.ts:385-388 `POST /reset` fa `unlinkSync(CONFIG_PATH)` (cancella la config arbitrariamente); setup.ts:355-383 `POST /finish` persiste un `b…
- **Verifica**: Ogni fatto tecnico regge end-to-end. Nessun middleware di auth/Origin/Host in tutta la catena: index.ts:9-23 monta app.route("/api", api) e api.ts:12-23 `api.route("/setup", setupRoutes)` senza guardie (grep auth/token/bearer/origin/cors su src/ = zero hit); i test stessi (tests/setup.test.ts:92-138) chiamano /api/setup/status|finish|reset via fetch nudo. install-cli → installCli() (setup.ts:187-201) esegue le pipeline fixed `irm ...|iex` / `curl ...|bash` come utente del processo; /reset (setup…
- **Fix suggerito**: —
- **Patch proposta**: [security-security-1.1-16.patch](patches/AUDIT-20260907-193547/security-security-1.1-16.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### security-1.2 — Server loopback senza validazione Host/Origin: DNS rebinding rende la superficie raggiungibile da qualsiasi sito
- **File**: `src/server/index.ts:19`
- **Rilevato da**: security · **Verificato da**: verifier indipendente (batch src/server/index.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: src/server/index.ts:19 `).listen(PORT, "127.0.0.1")` e index.ts:23 `serve({ fetch: app.fetch, port: PORT, hostname: "127.0.0.1" })` limitano il binding, ma nessun handler valida `Host`: `grep -rn "cors\|Origin\|Host" src/` → 0 hit. Senza check di Host, una pagina web arbitraria può fare DNS rebinding (rebind del proprio dominio su 127.0.0.1): il browser valuta la richiesta come same-origin, quindi la same-origin policy e l'assenza di CORS non bloccano né la lettura delle risposte né le POST JSON. Risultato: un drive-by da browser esegue le azioni di security-1.1 (install-cli/reset/finish) e le…
- **Verifica**: Verificato end-to-end sul codice reale e empiricamente. (1) Nessuna validazione Host/Origin: grep su src/ = 0 hit; index.ts ha solo serveStatic, api.ts/setup.ts nessuna guardia. (2) @hono/node-server (dist/index.js:222-237) valida solo la SINTASSI dell'header Host e lo usa verbatim per costruire l'URL — qualunque host è accettato. (3) Test live (server production su :3778): richiesta a 127.0.0.1 con 'Host: evil.com' (forma esatta di una richiesta rebinding da browser) → 200 con contenuto reale s…
- **Fix suggerito**: Middleware Host/Origin allowlist come primo handler dell'app Hono (sia dev che produzione): `app.use('*', async (c, next) => { const host = c.req.header('host'); if (host && !/^127\.0\.0\.1(:\d+)?$/.test(host) && !/^localhost(:\d+)?$/.test(host)) return c.text('forbidden', 403); return next(); })`. In alternativa abilitare `@hono/node-server` dietro un check su `req.socket.remoteAddress === '127.0
- **Patch proposta**: [security-security-1.2-17.patch](patches/AUDIT-20260907-193547/security-security-1.2-17.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### security+performance-3.2 — record-fixtures.mjs ignores ANILIST_FIXTURES, so recording can silently replay existing fixtures instead of hitting the API
- **File**: `scripts/record-fixtures.mjs:17`
- **Rilevato da**: security+performance · **Verificato da**: verifier indipendente (batch scripts/record-fixtures.mjs)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: anilist.ts:219/269/292/307 branch to fixture replay whenever `ANILIST_FIXTURES` is truthy (config.ts:15 reads it, and config.ts:4-11 auto-loads it from .env). record-fixtures.mjs:17/23/33 call `fetchUserList`/`fetchCandidates`/`fetchRecommendations` without clearing that env, and the README documents exactly this export (`README.md:37: ANILIST_FIXTURES=fixtures pnpm dev`, .env.example line "# ANILIST_FIXTURES=fixtures"). With the variable still exported, the script prints "fetching list of X… N entries", then rewrites fixtures/*.json from the very same files — a byte-identical no-op that looks…
- **Verifica**: Riprodotto end-to-end: con ANILIST_FIXTURES=fixtures esportato, `node scripts/record-fixtures.mjs someuser` (username inesistente) ritorna i dati sintetici del fixture, riscrive i 3 JSON e stampa "fixtures written" con exit 0 — zero chiamate API. Catena verificata su codice reale: config.ts:15 (`ANILIST_FIXTURES = process.env.ANILIST_FIXTURES ?? ""`, valutato all'import, con auto-load .env da .env.example che documenta proprio la chiave) → anilist.ts branch `if (ANILIST_FIXTURES)` in fetchUserLi…
- **Fix suggerito**: In record-fixtures.mjs, before importing/using the client: `if (process.env.ANILIST_FIXTURES) { console.error("ANILIST_FIXTURES must be unset to record"); process.exit(1); }` (must run before config.ts is imported), and write to `join(process.env.ANILIST_RECORD_DIR ?? "fixtures", ...)` — or better, write to the same path expression the reader uses.
- **Patch proposta**: [security+performance-securityperformance-3.2-18.patch](patches/AUDIT-20260907-193547/security+performance-securityperformance-3.2-18.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### security+performance-3.8 — RATE_PER_MIN of 0 (or negative) wedges takeToken: no refill and sleep(Infinity)
- **File**: `src/server/anilist.ts:84`
- **Rilevato da**: security+performance · **Verificato da**: verifier indipendente (batch src/server/anilist.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: config.ts:16 `RATE_PER_MIN = Number(process.env.RATE_PER_MIN ?? 25)` accepts any number; anilist.ts:84 `const refillPerSec = RATE_PER_MIN / 60`. With RATE_PER_MIN=0, line 89 adds 0 forever, so `tokens >= 1` is only true for the initial 3 requests; line 95 `sleep(1000 / refillPerSec)` = `sleep(Infinity)`, which Node clamps to 1ms → a hot loop that spins forever and the request never completes or errors (negative values: refillPerSec < 0 makes `tokens` decrease and the loop never yields). No validation anywhere (grep: RATE_PER_MIN only appears in config.ts:16 and anilist.ts:11,84).
- **Verifica**: Aritmetica verificata: config.ts:16 accetta qualunque numero, anilist.ts:84 `refillPerSec = RATE_PER_MIN/60`; con 0 il refill e' nullo (solo i 3 token iniziali) e `sleep(1000/0)=sleep(Infinity)`, con NaN (basta un valore .env non numerico, Number() su stringa) `tokens` diventa NaN e sleep(NaN) — provato su Node 22 che tutti questi delay vengono clamppati a ~1ms: hot loop permanente, nessuna richiesta completa mai, piu' loop concorrenti che spinnano. Impatto alto (server muto finche' non lo riavv…
- **Fix suggerito**: Clamp at the definition site so all callers share it: `export const RATE_PER_MIN = Math.max(1, Number(process.env.RATE_PER_MIN ?? 25));` in config.ts, or in anilist.ts: `const refillPerSec = Math.max(0.1, RATE_PER_MIN / 60);`
- **Patch proposta**: [security+performance-securityperformance-3.8-19.patch](patches/AUDIT-20260907-193547/security+performance-securityperformance-3.8-19.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### test-quality-8.5 — Franchise chains longer than MAX_DEPTH=10 untested: truncation silently misclassifies long series
- **File**: `tests/scoring.test.ts:181`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/scoring.test.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: All three franchise tests use chains of length 2-3 (scoring.test.ts:128-131, 138-142, 182-185). src/server/franchise.ts:16 `const MAX_DEPTH = 10;` caps the prequel walk (`for (let hop = 0; hop < MAX_DEPTH; hop++)`, :40), so for a 12+ entry franchise the chain only holds the 10 nearest prequels: a user who completed the nearest 10 but not the true early seasons gets `firstUnseen == null` → kind "NEXT_STEP" even though entries #1-#2 are unwatched (and the true root is lost, so rootId/dedupe grouping splits too). Long-running anime franchises (Monogatari, Conan, super-robot sagas) are the common …
- **Verifica**: Reproduced empirically, not just argued: built a 13-node PREQUEL chain in-pool with listMap marking 3..12 COMPLETED and 1-2 absent. Result for id 13 = {kind: "NEXT_STEP", rootId: 3, entryPointId: null} — exactly as claimed: the user is told S13 is the next step while entries 1-2 are unseen, and the true root is lost so the same franchise splits into two dedupe groups (rootId 1 for nodes 1/2/3, rootId 3 for 3..13). This violates the invariant the suite itself pins in test 7 ('never show S3 withou…
- **Fix suggerito**: Build a 13-node chain (media(i).relations = [{id: i-1, relationType: "PREQUEL"}]) with listMap marking 3..11 COMPLETED and 1-2 absent, then assert the documented intent for id 13 — e.g. it must not be NEXT_STEP (currently it is, which is a bug the test should either pin as EXPECTED or expose), and that rootId falls back consistently. At minimum add a comment+test asserting the deliberate truncatio
- **Patch proposta**: [test-quality-test-quality-8.5-20.patch](patches/AUDIT-20260907-193547/test-quality-test-quality-8.5-20.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

#### test-quality-9.2 — Test di setup non isolato dalla rete reale: la sonda HTTP su 127.0.0.1:1234 (LM Studio) bypassa il fake lms
- **File**: `tests/setup.test.ts:129`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/setup.test.ts)
- **Severità**: MEDIUM
- **Descrizione/evidenza**: Il test isola solo il binario via `LMS_PATH: fakeLms` (riga 75) ma run() (src/server/setup.ts:249-253) prima sonda `${cfg.baseUrl ?? LMSTUDIO_BASE}/models` con `const LMSTUDIO_BASE = "http://127.0.0.1:1234/v1"` (setup.ts:23): se qualcosa risponde su 1234 — esattamente la macchina-tipologia-utente di questa app, un dev con LM Studio attivo — `run()` fa early-return e non invoca MAI il fake lms, quindi le assert a righe 129-132 (`calls.includes("daemon up")` ecc.) falliscono dopo aver bruciato i 20s del loop (righe 118-128). Flaky per costruzione su ambiente, non sul codice.
- **Verifica**: Confermato end-to-end sul codice: /api/setup/finish scrive baseUrl: LMSTUDIO_BASE (setup.ts:378); run() (setup.ts:249-253) sonda il VERO http://127.0.0.1:1234/v1/models — const hardcoded senza override env (grep: solo setup.ts:23,249,332,378). Un 2xx → early-return backendState='up' PRIMA di resolveLms: il fake lms (LMS_PATH, test riga 75) non è mai invocato, callsPath non viene creato, e le assert a righe 129-132 falliscono dopo aver consumato l'intero deadline di 20s (righe 118-128). Il meccan…
- **Fix suggerito**: Rendere LMSTUDIO_BASE sovrascrivibile da env (es. `process.env.LMSTUDIO_BASE_URL ?? "http://127.0.0.1:1234/v1"` in setup.ts) e nel test puntarlo a una porta chiusa (binding effimero su porta 0 poi rilasciata), così il percorso daemon→server→get→load è deterministico.
- **Patch proposta**: [test-quality-test-quality-9.2-21.patch](patches/AUDIT-20260907-193547/test-quality-test-quality-9.2-21.patch)
- **Rischio residuo se non corretto**: degrado silenzioso di qualità/correttezza su path realistici

### LOW

#### contract-7.1 — postSetup promette { error?: string } risolto, ma l'helper json() throwa su ogni non-2xx: il campo error è irraggiungibile
- **File**: `src/ui/api.ts:39`
- **Rilevato da**: contract · **Verificato da**: verifier indipendente (batch src/ui/api.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: src/ui/api.ts:36-44 dichiara `postSetup = (...): Promise<{ ok?: boolean; error?: string }>`, ma l'helper a cui tutte le funzioni si appellano fa `if (!res.ok) throw new Error(body.error ?? `HTTP ${res.status}`)` (src/ui/api.ts:5). Il producer ritorna OGNI errore setup con status non-2xx e body `{ error: ... }`: `return c.json({ error: String((e as Error).message) }, 409)` (src/server/setup.ts:339), `{ error: "invalid_request" } 400` (setup.ts:345), `{ error: msg }, msg === "busy" ? 409 : 400` (setup.ts:351), `{ error: "invalid_url" } 400` (setup.ts:365), `{ error: "invalid_model" } 400` (setup…
- **Verifica**: Nucleo verificato end-to-end su codice reale. (a) Il producer ritorna OGNI errore come non-2xx con body {error}: setup.ts:339 (409), 345 (400), 351 (409/400), 359 (400), 365 (400), 371 (400), 373 (400). (b) Tutte le risposte 2xx sono esattamente {ok:true} (setup.ts:337, 348, 362, 369, 382, 391). (c) json() (api.ts:5) fa throw su ogni !res.ok, quindi il campo error?: string dichiarato a api.ts:39 è matematicamente irraggiungibile — nessun valore risolto lo popola mai, e nessun caller lo legge mai…
- **Fix suggerito**: Allineare il contratto al comportamento reale. Opzione A (tipo onesto, zero logica nuova): cambiare la firma in `Promise<{ ok: boolean }>` e documentare che gli errori arrivano come Error con il codice server nel messaggio. Opzione B (contratto rispettato davvero): far risolvere gli errori come dati, es. ```ts
export const postSetup = async (action, body?) => {
  const res = await fetch(`/api/setu
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### contract-7.2 — json() ritorna Promise<any> e può risolvere {} mentre ogni funzione promette una shape tipata (SetupStatus/RecoResult/{profile})
- **File**: `src/ui/api.ts:3`
- **Rilevato da**: contract · **Verificato da**: verifier indipendente (batch src/ui/api.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: src/ui/api.ts:3-7: `const json = async (res: Response): Promise<any> => { const body = await res.json().catch(() => ({})); if (!res.ok) throw ...; return body; }`. Su 2xx non c'è nessuna validazione: se il parse JSON fallisce il body risolto è `{}`, castato implicitamente al tipo dichiarato dal chiamante. Caso concreto: `fetchSetupStatus = (): Promise<SetupStatus> => fetch("/api/setup/status").then(json)` (api.ts:33-34) può risolvere `{}`; App.tsx:34-36 lo passa a `setSetup`, il guard `if (setup && !setup.setupDone && !setup.customEnv)` (App.tsx:107) è verificato (undefined falsy) e il render …
- **Verifica**: Il debito tipizzante è reale e verificato: json() (api.ts:3-7) è Promise<any>, res.json().catch(() => ({})) può risolvere {} su body non-JSON, e nessuna delle 6 funzioni esportate valida a runtime la shape — tutto è cast implicito al tipo dichiarato (SetupStatus, {profile: TasteProfile}, ecc.). MA la catena di crash citata non regge end-to-end sul produttore reale: l'unico handler di /api/setup/status è setup.ts:312-330 e ritorna SEMPRE 2xx con l'oggetto completo della shape giusta; ogni eccezio…
- **Fix suggerito**: Aggiungere una guardia minima per-endpoint invece del cast any, es. ```ts
const isSetupStatus = (b: any): b is SetupStatus =>
  b && typeof b.setupDone === "boolean" && !!b.hardware && !!b.job && Array.isArray(b.downloadedModels);

export const fetchSetupStatus = async (): Promise<SetupStatus> => {
  const b = await fetch("/api/setup/status").then(json);
  if (!isSetupStatus(b)) throw new Error("b
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### contract-7.3 — SetupStatus/SetupHardware in shared/types.ts è usato solo dalla UI: il producer (setup.ts) mantiene le proprie copie Hardware/SetupJob e risponde con un letterale non tipizzato
- **File**: `src/shared/types.ts:108`
- **Rilevato da**: contract · **Verificato da**: verifier indipendente (batch src/shared/types.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: types.ts:101-117 dichiara `SetupHardware` e `SetupStatus` (con `job: { state: ... ; model: string | null; logTail: string; error?: string }` e `llm: { state: "up" | "starting" | "off" }`) come tipo condiviso server/UI, ma il producer non lo referenzia: `grep -rn SetupStatus src/server/` non ha match. src/server/setup.ts ridefinisce `export interface Hardware { os; chip; ramGb; appleSilicon }` (setup.ts:27-32) e `export interface SetupJob { state: JobState; model: string | null; logTail: string; error?: string }` (setup.ts:152-157), e la risposta è costruita come oggetto letterale senza annotaz…
- **Verifica**: Ogni claim fattuale regge sul codice reale: zero riferimenti a SetupStatus/SetupHardware sotto src/server/ (consumatori solo in src/ui/); setup.ts ridefinisce Hardware (27-32), SetupJob (152-157) e le unioni JobState/BackendState (150, 222); la route /status (setup.ts:320-329) restituisce un letterale non annotato. Verifiche empiriche eseguite: (1) una probe temporanea che ricostruisce il letterale con gli helper reali annotata `satisfies SetupStatus` passa tsc --noEmit, quindi le due copy sono …
- **Fix suggerito**: Fare importare il tipo condiviso al producer: `import type { SetupStatus } from "../shared/types.ts"` in setup.ts, cancellare `Hardware`/`SetupJob` locali (riusare SetupHardware e il tipo del campo job, eventualmente esportando `SetupJob` da types.ts) e annotare la route: `setupRoutes.get("/status", async (c): Promise<Response> => c.json({...} satisfies SetupStatus))` — così ogni drift setup.ts ↔ 
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### dependency-13.3 — Transitive dependency caniuse-lite is CC-BY-4.0, not MIT-compatible, in a repo declared MIT with no third-party notice
- **File**: `pnpm-lock.yaml:496`
- **Rilevato da**: dependency · **Verificato da**: verifier indipendente (batch pnpm-lock.yaml)
- **Severità**: LOW
- **Descrizione/evidenza**: pnpm-lock.yaml:496 `caniuse-lite@1.0.30001810`; its installed package.json declares license `CC-BY-4.0` (node_modules/.pnpm/caniuse-lite@1.0.30001810/...), pulled in transitively via vite -> browserslist -> caniuse-lite (pnpm-lock.yaml:1024 `caniuse-lite: 1.0.30001810` under browserslist@4.28.9). CC-BY-4.0 carries attribution requirements and is treated as non-MIT-compatible for code distribution (FSF considers it GPL-incompatible), while the repo declares `"license": "MIT"` (package.json:6) and ships an MIT LICENSE file with no third-party license notice. Mitigating: it is a dev-only build-ti…
- **Verifica**: Tutti i fatti verificati sul codice reale: pnpm-lock.yaml:496 contiene caniuse-lite@1.0.30001810, il package.json installato dichiara 'CC-BY-4.0' (verificato con node), package.json:6 dichiara 'MIT', LICENSE è MIT puro e nessun file (README, docs/8 file, LICENSE) contiene una third-party notice — l'unico 'CC-BY-4.0' del repo è in docs/roadmap.md:3 e riguarda un dataset HF pianificato per v2, non le dipendenze. La mitigazione dev-only è reale, non assunta: grep su dist/assets/ (index-BslDtIT7.js …
- **Fix suggerito**: No dependency change needed (removing caniuse-lite would require dropping vite's browserslist chain). Make the license picture explicit instead: add a THIRD-PARTY-NOTICES.md (or docs section) listing dev-time assets under non-MIT terms, e.g. "caniuse-lite (CC-BY-4.0) — build-time browser support data via browserslist/vite, not distributed in the application bundle". Can be generated mechanically a
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-11.1 — MiniBar non limita la larghezza e la scala community ×10 è accoppiata in silenzio a WEIGHTS.communityCap
- **File**: `src/ui/components/RecoCard.tsx:10`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/ui/components/RecoCard.tsx)
- **Severità**: LOW
- **Descrizione/evidenza**: RecoCard.tsx:10 calcola `width: `${Math.round(props.value * 100)}%`` senza alcun clamp, e RecoCard.tsx:57 passa `value={r.breakdown.community * 10}`: la barra è corretta solo perché `WEIGHTS.communityCap` vale 0.1 (src/server/config.ts:75 `communityCap: 0.1`, con `comm = Math.min(WEIGHTS.communityCap, ...)` in scoring.ts:163). Il CSS non protegge: `.mini-bar` (styles.css:289-294) NON ha `overflow: hidden`, al contrario di `.dim-bar` di ProfilePanel (styles.css:170 `overflow: hidden`) che inoltre clampata in JS (ProfilePanel.tsx:5 `Math.min(100, Math.abs(props.d.aff) * 100)`). Quindi se il cap …
- **Verifica**: Tutte le affermazioni fattuali verificate sul codice: RecoCard.tsx:10 calcola la width senza clamp; RecoCard.tsx:57 passa breakdown.community*10; la correttezza dipende interamente da WEIGHTS.communityCap=0.1 (config.ts:75) applicato in scoring.ts:163; .mini-bar (styles.css:289-294) non ha overflow:hidden a differenza di .dim-bar, e MiniBar è l'unica barra senza clamp JS (ProfilePanel.tsx:5 lo ha). MA nessun difetto manifestabile oggi: community è doppiamente cappezzato a 0.1 (recommend.ts:89 + …
- **Fix suggerito**: Clampare dentro MiniBar (vale per tutte e tre le barre): `const pct = Math.min(100, Math.max(0, Math.round(props.value * 100)));` e usare `width: `${pct}%``. Opzionale: rendere esplicita la scala, es. `value={Math.min(1, r.breakdown.community / 0.1)}` con commento che lega al cap, così il ×10 magico non è l'unico posto che sa del valore 0.1.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-11.2 — ProfilePanel mostra l'enum della dimensione grezzo invece delle label i18n esistenti
- **File**: `src/ui/components/ProfilePanel.tsx:9`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/ui/components/ProfilePanel.tsx)
- **Severità**: LOW
- **Descrizione/evidenza**: ProfilePanel.tsx:8-10 renderizza `{props.d.dim}: {props.d.value}` dove `d.dim` è l'enum "tag"|"genre"|"studio"|"era" (src/shared/types.ts, `export type Dim = ...`). Le chiavi di traduzione dedicate esistono già e sono usate altrove: strings.ts definisce `dimTag`/`dimGenre`/`dimStudio`/`dimEra` (it: "tag", "generi", "studio", "epoca") e vengono usate in scoring.ts (deterministicWhy). Risultato: nell'UI italiana il pannello mostra "era: 1995" / "genre: ..." mentre il testo delle raccomandazioni dice "epoca"/"generi" — stessa entità, due etichette.
- **Verifica**: Verificato sul codice: ProfilePanel.tsx:9 renderizza l'enum grezzo `{props.d.dim}: {props.d.value}` (Dim = "tag"|"genre"|"studio"|"era", types.ts:48) mentre le label i18n dimTag/dimGenre/dimStudio/dimEra esistono in strings.ts (en :51-54, it :135-138) e non sono usate da nessuna UI — l'unico uso fuori dal dizionario è la fallback in scoring.ts:102. Conferma indiretta: DimBar riceve `lang` e non la usa mai. `tr` (strings.ts:177-180) ritorna la chiave se mancante, quindi il fix proposto è crash-sa…
- **Fix suggerito**: `<span className="dim-label">{tr(lang, `dim${d.dim[0].toUpperCase()}${d.dim.slice(1)}`)}: {d.value}</span>` (la fallback di `tr` ritorna la chiave se manca, quindi nessun rischio di crash).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-11.3 — Stato invalido/busy del form solo sul button disabled: nessun feedback accessibile né maxLength
- **File**: `src/ui/components/UsernameForm.tsx:30`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/ui/components/UsernameForm.tsx)
- **Severità**: LOW
- **Descrizione/evidenza**: UsernameForm.tsx:10 `const valid = /^[A-Za-z0-9_-]{1,32}$/.test(value)` e :30 `<button type="submit" disabled={!valid || props.busy}>`: l'unico segnale per l'utente è il button disabilitato. Non c'è `maxLength={32}` sull'input (:21-29), né `aria-invalid`, né testo/`aria-describedby` che spieghi la regola consentita. Un utente che incolla un username con punto/spazio/accento (es. "mario.rossi", "José") o >32 caratteri vede solo un button grigio senza motivo; per screen reader lo stato di errore è invisibile (l'input non è marcato invalid).
- **Verifica**: Verificato su UsernameForm.tsx: l'input (:21-29) non ha maxLength, aria-invalid, pattern o hint; l'unico feedback è il button disabled (:30, opacity 0.5, non focusable). Nessuna mitigazione a monte: strings.ts non contiene alcuna stringa che spieghi la regola username (le uniche stringhe errore, errUserNotFound/errAnilistDown/errGeneric, si mostrano in App.tsx solo dopo una submit valida con fetch fallita), e Invio nell'input viene scartato in silenzio dal guard :16. Nessun test UI esiste (tests…
- **Fix suggerito**: Aggiungere `maxLength={32}` e `aria-invalid={!valid && value.length > 0}` all'input, più un messaggio (`role="status"` o `aria-describedby="username-hint"`) con la regola ammessa; in alternativa lasciare il button abilitato e mostrare l'errore al submit invece del silenzio.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-11.4 — Bootstrap senza controllo del mount node né error boundary: errori di render spariscono in pagina vuota
- **File**: `src/ui/main.tsx:5`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/ui/main.tsx)
- **Severità**: LOW
- **Descrizione/evidenza**: main.tsx:5 `createRoot(document.getElementById("root")!).render(<App />);` — la non-null assertion trasforma un mount mancante in un generico "Cannot read properties of null (reading 'render')" senza indicare la causa; e non esiste alcun error boundary: un'eccezione in render (es. payload con shape inattesa che tocca `r.breakdown.affinity` in RecoCard.tsx:55 o `p.counts.*` in ProfilePanel.tsx:26) smonta l'albero e lascia la pagina completamente bianca, senza alcun messaggio — fallimento silenzioso per l'utente finale.
- **Verifica**: Verificato su codice reale: src/ui/main.tsx:5 è `createRoot(document.getElementById("root")!).render(<App />)` senza guardia sul mount né wrapper; grep su src/ e tests/ conferma ZERO ErrorBoundary/componentDidCatch nel repo, e nessun test UI esiste. In React 19 un render error non catturato smonta l'intero albero → pagina bianca senza alcun feedback: il meccanismo denunciato regge. Però i trigger citati NON sono raggiungibili col contratto attuale: (1) index.html e dist/index.html contengono ent…
- **Fix suggerito**: `const el = document.getElementById("root"); if (!el) throw new Error("#root mancante in index.html"); createRoot(el).render(<ErrorBoundary fallback={<p>Errore inatteso. Ricarica.</p>}><App /></ErrorBoundary>);` con una ErrorBoundary minimale (component class con componentDidCatch) nello stesso file o vicino.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-12.1 — Focus ring del link copertina clippato da overflow:hidden sulla card
- **File**: `src/ui/styles.css:215`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/ui/styles.css)
- **Severità**: LOW
- **Descrizione/evidenza**: .reco-card { display: grid; grid-template-columns: 110px 1fr; background: var(--surface); border-radius: var(--radius); overflow: hidden; } (215-216). .cover-link e' un grid item stretch a piena altezza con img { width:100%; height:100% } (217-222), quindi il suo border-box coincide con i bordi della card. La regola globale :is(button, select, input, a):focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; } (106-109) disegna l'outline 2px FUORI dal box: su top/left/bottom cade oltre il bordo della card e viene tagliato da overflow:hidden; resta visibile solo una sliver di 2px…
- **Verifica**: Verificato su styles.css: .reco-card (210-216) ha overflow:hidden + border-radius e nessun padding/border, .cover-link (RecoCard.tsx:22) e' grid item stretch nella colonna 110px quindi il suo border-box e' flush con top/left/bottom della card; la regola globale :is(button,select,input,a):focus-visible (106-109) con outline-offset:+2px dipinge fuori dal box e un antenato overflow:hidden clippa l'ink overflow dei discendenti → 3 lati su 4 persi. Resta solo il segmento interno di 2px a x=110px, eff…
- **Fix suggerito**: Disegnare l'anello dentro il box solo per questo link: .cover-link:focus-visible { outline-offset: -3px; } (alternativa: rimuovere overflow:hidden da .reco-card e applicare border-radius all'img figlio).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-12.2 — Manca color-scheme: dark: i controlli nativi rendono in scheme light su tema dark-only
- **File**: `src/ui/styles.css:1`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/ui/styles.css)
- **Severità**: LOW
- **Descrizione/evidenza**: :root (1-12) definisce solo custom properties scure; grep di color-scheme|prefers-color-scheme|accent-color|theme-color su src/ e index.html = 0 match. Non esiste tema chiaro: l'app e' dark-only. Conseguenza: il browser usa lo scheme di default (light) per le parti non stilizzabili da CSS -> il popup dei <select> (App.tsx:162,167), il checkbox (App.tsx:159, SetupWizard.tsx:85), la scrollbar di .joblog { overflow: auto } (398), caret e autofill rendono light (dropdown bianco, scrollbar chiara) su interfaccia scura.
- **Verifica**: Fatto accertato: grep -rniE 'color-scheme|prefers-color-scheme|accent-color|theme-color|light-dark' su src/, index.html e dist/index.html = 0 match; :root (1-12) ha solo custom properties, app dark-only. Quindi i widget UA (popup dei select App.tsx:162/167, checkbox App.tsx:159 e SetupWizard.tsx:86, scrollbar di .joblog styles.css:398) rendono in scheme light. Downgrade a MEDIUM->LOW: i controlli chiusi sono gia' restilizzati da styles.css:88-98 (color/background), il popup light e' perfettament…
- **Fix suggerito**: Aggiungere in :root: color-scheme: dark; (ed eventualmente accent-color: var(--accent); per il checkbox). Fix di una riga, allinea i widget UA al tema.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.10 — POST /finish silently drops an invalid model and completes setup anyway
- **File**: `src/server/setup.ts:367`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `if (body.model && MODEL_KEY_RE.test(body.model)) patch.model = body.model;` — when a client sends a custom baseUrl plus a model that fails the regex, the model is discarded without any error and `updateConfig(patch)` with `setupDone: true` proceeds, so the wizard reports success while config.model stays undefined (or keeps an older value via the merge in updateConfig) and llmModel() falls back to its default (src/server/config.ts:52).
- **Verifica**: Confermato a 364-370: con baseUrl valido e model che fallisce la regex, il modello è scartato senza errore, updateConfig scrive setupDone:true e risponde ok; llmModel() (config.ts:52) cade su 'qwen3:8b'. Nota che mitiga: la wizard invia solo chiavi del catalogo (regex-valid) o nessun model, quindi serve una richiesta manuale; e la conseguenza è LLM silenziosamente off con fallback deterministico, non un errore. LOW.
- **Fix suggerito**: Reject instead of dropping: `if (body.model != null && !MODEL_KEY_RE.test(body.model)) return c.json({ error: "invalid_model" }, 400);` before building the patch.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.11 — detectHardware: spawnSync stdout is null when sysctl cannot be spawned → TypeError in the /status route
- **File**: `src/server/setup.ts:40`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const { stdout } = spawnSync("sysctl", ["-n", "machdep.cpu.brand_string"], { encoding: "utf8" }); appleSilicon = stdout.includes("Apple");` — when spawnSync fails to spawn (ENOENT because sysctl is not on PATH, EACCES in a hardened sandbox), it returns `stdout: null` and the destructured call throws `Cannot read properties of null (reading 'includes')`. detectHardware() is called on every GET /setup/status (line 314), so the whole status endpoint 500s instead of just falling back to `appleSilicon = false`.
- **Verifica**: Meccanica verificata empiricamente: spawnSync su binario inesistente ritorna stdout undefined (non stringa vuota) → `stdout.includes('Apple')` lanza TypeError, che dentro detectHardware (chiamata in /status:314 e installCli:189) 500-erebbe l'endpoint. Però il branch gira solo su darwin x64 (37: mac && !arm64), dove /usr/sbin/sysctl esiste sempre ed è nel PATH di default anche per app lanciate da Finder → trigger di fatto irraggiungibile sulle piattaforme supportate. Difetto reale, debito non man…
- **Fix suggerito**: Guard the result: `appleSilicon = typeof stdout === "string" && stdout.includes("Apple");` (or check `r.error`/`r.status === 0` first).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.12 — backendState is write-once at boot/startup and never re-evaluated, so /status reports a stale "up" after the server dies
- **File**: `src/server/setup.ts:223`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `let backendState: BackendState = "off";` is only mutated inside ensureLlmServer's run() (lines 236, 240, 251, 265, 274, 283). If the LM Studio server exits or crashes minutes later, `llmBackendState()` — exposed to the wizard as `llm: { state: backendState }` (line 328) and in /api/health (src/server/api.ts:17) — keeps reporting "up" with no re-check anywhere, while the truth is only recomputable via the separate `serverUp`/`llmHealth()` probes.
- **Verifica**: Confermato: backendState è scritto solo dentro ensureLlmServer/run (236,240,251,265,274,283), mai riprobato; se il server muore dopo, il campo resta 'up'. Ma l'esposizione è minima: /status include già il probe fresco `serverUp` (318,325) accanto al campo stale, /api/health ha `enabled` da llmHealth (probe fresca) e la UI usa solo quello (App.tsx:45); l'unico consumatore di llm.state è il messaggio step-3 della wizard, visibile solo pre-setupDone. Campo diagnostico sbagliato, verità comunque dis…
- **Fix suggerito**: Either derive the wizard-facing state at read time (e.g. cache the last probe result with a short TTL alongside `serverUp` in /status) or drop backendState in favour of the existing health probe so the two cannot disagree.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.2 — Concurrent `lms get` race: auto-download in run() bypasses the job singleton
- **File**: `src/server/setup.ts:271`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: The wizard download path goes through the singleton (`startDownload` sets `job = { state: "downloading", ... }` line 174 and refuses concurrent runs at line 170), but ensureLlmServer's `run()` calls `runLms(lms, ["get", cfg.model!, ...], 60 * 60 * 1000)` directly (line 271) without touching `job`. While that up-to-1h auto-download runs, `job.state` stays "idle": /status reports `downloadedModels: models` (line 317 only short-circuits when `job.state === "downloading"`) and POST /download passes the busy check, spawning a second `lms get` — possibly for the same model — racing on the same downl…
- **Verifica**: Fatti di codice corretti: run() chiama runLms(['get',...], 1h) a setup.ts:271 senza toccare job; ensureLock (235) non è consultato da startDownload; /status cortocircuita solo su job.state==='downloading' (317). MA la catena d'impatto non regge via UI: la wizard non chiama mai /setup/download (il bottone fa finish({model}) → ensureLlmServer), e run() gira solo con setupDone=true (233), condizione in cui la wizard non è renderizzata (App.tsx:107). La race richiede una chiamata API manuale a un se…
- **Fix suggerito**: Route the auto-download through startDownload() (or a shared `beginDownload(model)` helper that owns the job singleton), so the busy check and /status visibility cover both paths; in run(), bail out if a job is already active.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.3 — runLms timeout only sends SIGTERM and never escalates; promise can never resolve and the job hangs
- **File**: `src/server/setup.ts:120`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const timer = setTimeout(() => { if (!done) { log(...); child.kill("SIGTERM"); } }, timeoutMs);` — if `lms` (or a downloader child it spawned) ignores or survives SIGTERM, no SIGKILL follows and `close` never fires, so `runLms`'s Promise never resolves. For the 1h download in startDownload (line 177) or the auto-download at line 271, the job stays "downloading" indefinitely (startDownload's `.then` never runs), the ensure lock is held forever (`ensureLock` cleared only in `.finally`, line 242), and the busy guard blocks /download permanently.
- **Verifica**: Il codice conferma: il timer manda solo SIGTERM (setup.ts:123), nessuna escalation a SIGKILL, e `close` non parte se il processo (o un figlio che eredita le pipe) ignora il segnale → la Promise non si settle mai; per il download in run() anche .finally di ensureLock (242) non gira e il lock resta presio per sempre. Meccanismo end-to-end corretto, ma il trigger dipende da comportamento esterno di lms (ignori SIGTERM / nipoti sopravvissuti) non dimostrabile dalla repo: LOW.
- **Fix suggerito**: Escalate: after SIGTERM, `setTimeout(() => child.kill("SIGKILL"), 5000)`; optionally resolve the Promise with `code: -1` after a post-kill grace window if `close` still has not fired.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.4 — Configured-model presence check uses substring match; model-present false positive skips a needed download
- **File**: `src/server/setup.ts:269`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `if (!ls.stdout.includes(cfg.model!))` tests raw `lms ls --json` output as a plain string. Any other key in the JSON that merely contains the configured key as a substring makes the download be skipped — e.g. cfg.model="prism-ml/Bonsai-8B" with only "prism-ml/Bonsai-8B-gguf" installed, or cfg.model="prism-ml/Bonsai-8B-gguf" when only a longer variant key exists — after which `lms load <cfg.model>` fails (logged and ignored, line 279) and the model is never actually available.
- **Verifica**: Confermato a riga 269: `ls.stdout.includes(cfg.model!)` su JSON grezzo; riprodotto il false-positive (installed 'prism-ml/Bonsai-8B-gguf-mlx' maschera l'assenza di 'prism-ml/Bonsai-8B-gguf'), e il parse corretto esiste già in downloadedModels (297-300). Però col catalogo effettivo (4 chiavi fisse in MODELS + 2 mlx, uniche offerte dalla wizard) nessuna chiave è substring di un'altra, quindi il difetto non è manifestabile con la config navigabile oggi; direzione opposta (ls fallito → stdout '' → d…
- **Fix suggerito**: Parse the JSON (reuse the parsing in downloadedModels(), lines 296-300) and compare keys exactly: `const keys = parse(ls.stdout).models.map(m => m.key ?? m.path); if (!keys.includes(cfg.model)) ...`.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.5 — load failure swallowed and /v1/models 200 treated as success: backendState "up" with no model loaded
- **File**: `src/server/setup.ts:279`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `if (load.code !== 0) log(...)` continues instead of failing; the poll loop then checks `httpOk(`${base}/models`, ...)` (lines 280-283). LM Studio answers 200 on /v1/models even with zero models loaded, so `backendState = ... ? "up" : "off"` (line 283) reports "up" while every chat/completions call for cfg.model fails. Same masking at the top of run(): `if (await httpOk(`${base}/models`, 2000)) { backendState = "up"; return; }` (lines 250-253) declares "up" without ever loading cfg.model when any server already occupies the port.
- **Verifica**: Codice confermato: load.code!==0 solo loggato (279), successo giudicato solo da httpOk(/models) (283), e early-return 250-253 dichiara 'up' per qualunque server occupi la porta senza caricare cfg.model. La premessa esterna (LM Studio risponde 200 su /v1/models a modello non caricato) non è verificabile dalla repo ma è comportamento standard OpenAI-compat. Impatto reale limitato: wizard step-3 dice 'ready' e il chip dice LLM on a torto, ma ogni failure di chat finisce nel catch di explainRecos (l…
- **Fix suggerito**: Treat a nonzero `load.code` as fatal (set backendState "off" and log the stderr tail), and gate "up" on the loaded-model list (`GET /v1/models` body containing cfg.model) rather than on res.ok alone.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.6 — POST /setup/reset swallows all unlink errors, reports ok, and leaves in-memory config/job state stale
- **File**: `src/server/setup.ts:385`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `try { unlinkSync(CONFIG_PATH); } catch { /* already gone */ }` swallows every failure (EPERM/EACCES/EBUSY), yet the route always returns `{ ok: true }` — the catch's comment only covers ENOENT. Additionally the module-level `fileConfig` in src/server/config.ts (lines 42, 49) is refreshed only by updateConfig, so after a reset `llmModel()`/`llmBaseUrl()` keep serving the deleted config's values until the next updateConfig or restart; `job` (line 159) and `backendState` (line 223) are untouched, and a running download/install child keeps going.
- **Verifica**: Confermato: catch{} a 385-392 ingoia ogni errno (non solo ENOENT) e risponde sempre ok:true; fileConfig (config.ts:42,49) è refreshato solo da updateConfig, quindi dopo reset llmModel()/llmBaseUrl() (usati da llmChat, /api/config, /api/health) continuano a servire i valori della config cancellata; job e backendState intatti. Impatto però corto e benigno: la finestra stale va dal reset al prossimo updateConfig, con valori che funzionavano un attimo prima, e readConfigFile su disco (usato da ensur…
- **Fix suggerito**: Only swallow ENOENT (`if ((e as NodeJS.ErrnoException).code !== "ENOENT") { return c.json({ error: "reset_failed" }, 500); }`), and after a successful unlink also reset the shared state: updateConfig({}) to clear fileConfig, reset `job` to idle and `backendState` to "off" (needs a small exported resetState() in setup.ts, plus clearing fileConfig in config.ts).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.7 — Log path built with new URL(...).pathname: broken on Windows and any path with spaces/encoded chars, and the failure is silently swallowed
- **File**: `src/server/setup.ts:14`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const LLM_LOG = new URL("../../data/llm.log", import.meta.url).pathname;` and `mkdirSync(new URL("../data/", import.meta.url).pathname, ...)` (line 87). `.pathname` returns percent-encoded text ("My%20Docs") and on win32 "/C:/Users/...", which fs APIs treat as a relative literal path. Result: mkdirSync/appendFileSync throw (or write to a bogus "%20"/"C:" directory) and the `catch { /* logging must never crash the app */ }` (lines 89-91) hides it, so all setup/LLM diagnostics silently disappear. The same pattern for CACHE_DIR (src/server/config.ts:59) breaks the explanation cache the same way.
- **Verifica**: Riprodotto empiricamente su macOS: con uno spazio nel percorso, `new URL('../../data/llm.log', import.meta.url).pathname` = '/tmp/alr%20space%20test/data/llm.log'; mkdirSync(recursive) crea silenziosamente la dir-fantasma letterale '%20' e appendFileSync fallisce ENOENT, ingoiato dal catch (89-91) → llm.log perso senza traccia. Stesso pattern per CACHE_DIR (config.ts:59) che disabilita la cache spiegazioni. Colpisce però solo path di clone con spazi/caratteri encodabili (caso win32 '/C:/...' non…
- **Fix suggerito**: Use `import { fileURLToPath } from "node:url"` and `join(fileURLToPath(new URL("../../data", import.meta.url)), "llm.log")` (same for the data dir and CACHE_DIR in config.ts); optionally log a one-time warning when the sink cannot be created.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-2.8 — downloadedModels caches a failed/empty parse result as truth for 10s
- **File**: `src/server/setup.ts:301`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `catch { /* non-json output (older lms) — treat as unknown */ }` leaves `models = []`, then line 304 unconditionally does `lsCache = { at: Date.now(), models }; return models;`. A transient `lms ls` failure (runLms resolves code -1 on spawn error, line 138), a timeout, or non-JSON output is cached as "no models downloaded" for 10s (checked at line 293), so /status tells the wizard nothing is installed and, since "error" is indistinguishable from "empty", the UI can push the user to re-download an already-present model.
- **Verifica**: Confermato: il catch a 301 lascia models=[] e la 304 cache-ifica incondizionatamente, con TTL 10s verificato alla 293; runLms risolve code -1 su spawn error (138) e code null su timeout, entrambi finiscono come 'nessun modello'. Effetto reale ma micro: /status (e la transizione di step della wizard) può dire 'non scaricato' per ≤10s dopo un failure transitorio, poi si auto-ripara. LOW.
- **Fix suggerito**: Only populate the cache on success (`if (ls.code === 0) lsCache = ...`) or store a nullable `models` so callers can distinguish unknown from empty; on failure return the previous cache if any.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-4.5 — Third loved genre is fetched but never queried: `genres = top(profile, "genre", 3)` while every query loop uses `genres.slice(0, 2)`
- **File**: `src/server/candidates.ts:22`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/server/candidates.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const genres = top(profile, "genre", 3);` (candidates.ts:19) but the only uses are `for (const g of genres.slice(0, 2))` (candidates.ts:22, popularity pages) and `for (const g of genres.slice(0, 2))` (candidates.ts:29, SCORE_DESC gem pages) — `genres[2]` is dead, contradicting the doc comment "top-3 loved genres x 2 pages popularity" (candidates.ts:12). With the ordering bug of logic-4.1 fixed, the 3rd strongest genre would still never generate candidates.
- **Verifica**: Confermato dal codice: `genres` è referenziato solo a candidates.ts:22 e :29, entrambe con slice(0, 2) — genres[2] è morto. La discrepanza col commento è aritmeticamente dimostrabile: 'top-3 loved genres x 2 pages' (6) + 5 tag + 2 gem = '~13 requests' come da commento, ma il codice emette 4+5+2 = 11. Il valore morto non è un errore manifestabile (il pool resta ampio, ~50 item/page), è solo copertura ridotta di ~2 query e documentazione mendace; rimane anche dopo un eventuale fix del 4.1. Impatto…
- **Fix suggerito**: Pick one: either request 3 genres (`genres.slice(0, 3)`) as the comment claims, or request only 2 (`top(profile, "genre", 2)`) so the dead value and the mismatched comment disappear.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-4.6 — Cycle guard keeps per-candidate roots: two candidates in a PREQUEL cycle never merge into one franchise group
- **File**: `src/server/franchise.ts:43`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/server/franchise.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: The guard `if (visited.has(p)) break;` (franchise.ts:43) stops the walk but leaves each candidate as its own chain head: verified repro with candidates 501↔502 (each listing the other as PREQUEL) gives `rootId 502` for 501 and `rootId 501` for 502. dedupeFranchises keys on rootId (scoring.ts:196-202), so both are kept as separate representatives with groupSize 1 — duplicate cards for the same series. Bad mutually-referencing PREQUEL data does occur on AniList (the author added the guard, so cycles are considered in-model).
- **Verifica**: Repro su franchise.ts:43: ciclo mutuo 501↔502 dà rootId 502 per 501 e 501 per 502 (ciclo a 3: tre root distinti, tutti ENTRY_POINT che si indicizzano a vicenda) — nessun merge possibile in dedupeFranchises, che chiave su rootId. Il test esistente (tests/scoring.test.ts:181 "relation cycle does not hang") verifica solo il non-blocco, non la convergenza dei root. Nuance end-to-end verificata: con entrambi non visti recommend.ts:50-64 li annichilisce a vicenda (entrambi ENTRY_POINT → entrambi super…
- **Fix suggerito**: Canonicalize the root by minimum id: after computing the chain, set `rootId: Math.min(m.id, ...chain)` (or union-find over prequel edges) so every member of a cyclic cluster shares one rootId and dedupeFranchises collapses them.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic-4.7 — whyNotDropped branch is unreachable in production: only EXCLUDED candidates carry droppedId, and EXCLUDED candidates are filtered out before scoring
- **File**: `src/server/scoring.ts:146`
- **Rilevato da**: logic · **Verificato da**: verifier indipendente (batch src/server/scoring.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: In analyzeFranchises `droppedId != null` implies `kind = "EXCLUDED"` (franchise.ts:58-59) and vice versa. recommend.ts scores only non-EXCLUDED candidates: `candidates.filter((c) => franchise.get(c.id)?.kind !== "EXCLUDED")` (recommend.ts:95), and the anti-recommendation list derives from that same filtered array: `const bottom = [...scored].filter(...)` (recommend.ts:113) → `const droppedTitle = f?.droppedId != null ? ... : null` (recommend.ts:120) is always null there. Hence `droppedPart: droppedTitle ? tr(lang, "whyNotDropped", ...) : ""` (scoring.ts:146) and the templates "…and you dropped…
- **Verifica**: Verificato end-to-end: in franchise.ts:50-67 `droppedId != null` implica per costruzione `kind === "EXCLUDED"` (bi-implicazione: EXCLUDED è assegnato solo in quel ramo). L'unico caller production di scoreAll è recommend.ts:94-95, che filtra via i candidati EXCLUDED; `bottom` (recommend.ts:113-116) deriva da quel medesimo array `scored` filtrato, quindi per ogni suo elemento `franchise.get(id)?.droppedId === null` e `droppedTitle` a recommend.ts:120 è sempre null. `deterministicWhyNot` ha un solo…
- **Fix suggerito**: Either feed EXCLUDED candidates into the avoided computation (build `bottom` from the unfiltered candidate list, keeping the filter only for `scored`/recommendations) so dropped-series entries surface with their droppedTitle, or delete the dead droppedId/droppedTitle plumbing from deterministicWhyNot and FranchiseInfo to remove the false affordance.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic+contract-5.2 — Acc key joins dim and value with ':' but splits with unbounded split(':') — values containing ':' are silently truncated and their affinity becomes unreachable
- **File**: `src/server/profile.ts:45`
- **Rilevato da**: logic+contract · **Verificato da**: verifier indipendente (batch src/server/profile.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: Write side: `const key = \`${dim}:${value}\`;` (profile.ts:45). Read side: `const [dim, value] = key.split(":") as [Dim, string];` (profile.ts:62) — `split(":")` without a limit drops everything after the second element, so a value with a colon is stored truncated, and the reconstruction used by the affinity lookup (scoring.ts:9 / scoring.ts:22 `am.get(\`${dim}:${value}\`)`) uses the full name, so it never matches. Reproduced: a user who rated three shows at 60 (genre "A") and two shows at 90 with genre "Sci-Fi: Cyber" gets `loved: [{dim:'genre', value:'Sci-Fi', aff:0.08, support:2}]` — the re…
- **Verifica**: Meccanica verificata e riprodotta: `${dim}:${value}` + `key.split(":")` (profile.ts:45/62) tronca 'Sci-Fi: Cyber' → loved riporta value 'Sci-Fi' (support 2), e affinityOf (scoring.ts:22) cerca 'genre:Sci-Fi: Cyber' → mai match; due valori distinti ('X' e 'X:Y') collidono nello stesso bucket mescolando sum/weight. Il valore mostrato in UI (ProfilePanel.tsx:9) sarebbe sbagliato. MA il difetto è latente, non manifestabile oggi: i valori che raggiungono addSentiment provengono solo da enum/tassonomi…
- **Fix suggerito**: Don't round-trip through a joined string. Either keep the Acc in a typed entry: `type Keyed = Acc & { dim: Dim; value: string }` with `acc: Keyed[]` and a helper find-or-push (values are few per user), or keep the Map but make the split asymmetry-safe: `const sep = key.indexOf(":"); const dim = key.slice(0, sep) as Dim; const value = key.slice(sep + 1);` — one line, restores the exact value for an
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic+contract-5.3 — Profile fingerprint hash omits `repeat`, which entrySentiment uses — the LLM explanation cache serves stale results for up to 7 days after a rewatch-flag change
- **File**: `src/server/profile.ts:120`
- **Rilevato da**: logic+contract · **Verificato da**: verifier indipendente (batch src/server/profile.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `entries.map((e) => \`${e.mediaId}:${e.status}:${e.score}\`).sort().join("|")` (profile.ts:119-122) excludes `repeat`, but sentiment depends on it: `const repeatBonus = WEIGHTS.repeatBonus * Math.min(e.repeat, WEIGHTS.repeatCap);` (profile.ts:26). The hash is the cache key of the LLM explanation store: `createHash("sha256").update(\`${username}|${profile.hash}|${lang}|${llmModel()}|\`)...` then `cacheGet(key)` / `cacheSet(key, ...)` with `CACHE_TTL_EXPL_MS = 7 * 24 * 60 * 60 * 1000` (src/server/llm.ts:109-113, config.ts). Reproduced: identical list with `repeat: 0` vs `repeat: 1` on one entry …
- **Verifica**: Verificato: hash = `${mediaId}:${status}:${score}` (profile.ts:120) è identico con repeat 0 vs 1 (3418142c6d5407e1 in entrambi i run) mentre la aff del dim toccato cambia (0.09 → 0.11, via repeatBonus profile.ts:26); l'hash è la cache key LLM (llm.ts:109-113, TTL 7d in config.ts:62) e su hit il testo cache vince sul 'why' deterministico fresco (llm.ts:114-118) → spiegazioni stale fino a 7 giorni. Downgrade a LOW perché il trigger è più stretto di quanto dichiarato: lo status È nella tuple, quind…
- **Fix suggerito**: Include the repeat count in the fingerprint tuple: `entries.map((e) => \`${e.mediaId}:${e.status}:${e.score}:${e.repeat}\`)` (profile.ts:120). This changes existing cache keys, which is the point — no migration needed since keys are opaque content-addressed files in data/cache/expl/.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic+contract-5.4 — support counts zero-weight contributions: a rank-0 tag inflates support and halves the shrinkage penalty without contributing to the affinity
- **File**: `src/server/profile.ts:49`
- **Rilevato da**: logic+contract · **Verificato da**: verifier indipendente (batch src/server/profile.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `a.sum += s * rankFactor; a.weight += w * rankFactor; a.support += 1;` (profile.ts:47-49) — for tags `rankFactor = t.rank / 100` (profile.ts:114), which is 0 when AniList returns rank 0 (rank is passed through unclamped in anilist.ts:185). Such an entry adds nothing to `sum`/`weight` yet still increments `support`, which drives both the gate `a.support < WEIGHTS.supportMin` (profile.ts:63) and the shrink factor `Math.min(a.support, WEIGHTS.supportShrink) / WEIGHTS.supportShrink` (profile.ts:64). Measured: a tag seen on one 95-rated entry at rank 100 plus one rank-0 entry gives aff 0.05 (shrink…
- **Verifica**: Meccanica confermata per via empirica: `a.support += 1` incondizionato (profile.ts:49) mentre rankFactor = t.rank/100 = 0 porta sum/weight invariati; rank passa senza clamp né filtro di soglia in mapMedia (anilist.ts:183-185, solo filtro spoiler). Riprodotto: un tag con 1 osservazione reale rank-100 + 1 osservazione rank-0 passa il gate supportMin=2 ed è ammesso in loved (aff 0.131, support 2) mentre la stessa singola osservazione da sola è scartata; con support pari il fattore di shrink raddopp…
- **Fix suggerito**: Only count support when the observation actually carries weight: `if (w * rankFactor > 0) a.support += 1;` (profile.ts:49), keeping the sum/weight lines unchanged. Era/genre/studio (rankFactor 1) and all non-PLANNING entries (w > 0 by profile.ts:28) are unaffected.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic+contract-5.5 — tr() interpolates param values as a replacement string, so '$'-patterns in titles ($$, $&, $`) are interpreted and corrupt the output
- **File**: `src/shared/strings.ts:179`
- **Rilevato da**: logic+contract · **Verificato da**: verifier indipendente (batch src/shared/strings.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `s = s.replaceAll(\`{${k}}\`, String(v));` (strings.ts:179). With a string search pattern, String.prototype.replace/replaceAll still interpret the special replacement patterns in the replacement string, and this function interpolates raw user/media data: `whyExamples` (`examples: examples.join(", ")`) and `whyNotDropped` (`{ title: droppedTitle }`) at scoring.ts:105/144/146 receive AniList titles verbatim (anilist.ts:179 `title: m.title.romaji`). A title containing `$$` renders as `$`, and `$&` re-inserts the matched `{key}` literal; a title containing `{gemPart}` or `{examplesPart}` literally…
- **Verifica**: Confermato end-to-end e riprodotto per esecuzione: strings.ts:179 usa String.prototype.replaceAll con pattern stringa, e per specifica JS la replacement string elabora comunque i pattern $ (String(v) arriva raw). Prove: ' Seen in: {examples}.'.replaceAll('{examples}', 'Dollar $$ & $& Test') → ' Seen in: Dollar $ & {examples} Test.'; backtick-pattern ($`) riproduce la porzione precedente; e l'iniezione di placeholder è reale e ordine-dipendente ('A{gemPart}B' con examplesPart='...{gemPart}...' e …
- **Fix suggerito**: Use a function replacement, which is copied verbatim: `s = s.replaceAll(\`{${k}}\`, () => String(v));` (strings.ts:179) — same length, removes both the `$`-pattern and placeholder-injection classes.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic+security-10.5 — Boot-time setup-status failure is conflated with 'setup done': wizard silently skipped
- **File**: `src/ui/App.tsx:36`
- **Rilevato da**: logic+security · **Verificato da**: verifier indipendente (batch src/ui/App.tsx)
- **Severità**: LOW
- **Descrizione/evidenza**: `fetchSetupStatus().then(setSetup).catch(() => setSetup(null));` and the gate `if (setup && !setup.setupDone && !setup.customEnv) return <SetupWizard …>` (line 107). A failed/transient request (server 500 while reading config, dev proxy hiccup) yields `null`, which the guard treats exactly like a completed setup — an unconfigured user lands in the main app with the LLM chip 'off' and only generic errors, and can never reach the wizard for that session. Same-shaped code for health (`catch(() => setLlmOn(false))`, line 46) at least degrades to an honest 'LLM off' chip; `setSetup(null)` degrades …
- **Verifica**: Logica confermata: .catch(() => setSetup(null)) (App.tsx:36) e gate if (setup && !setup.setupDone && !setup.customEnv) (linea 107) — null attraversa il gate come un setup completato; l'effect ha deps [] quindi nessun retry, e non esiste nessun altro ingresso al wizard nell'app (SetupWizard è reso solo da quel gate; l'azione 'reset' di postSetup è interna al wizard stesso). Però l'impatto reale è minore di quanto dichiarato dal finding: l'app resta pienamente funzionante (spiegazioni deterministi…
- **Fix suggerito**: Keep three states: `const [setup, setSetup] = useState<SetupStatus | null | "error">(null)` — render the app only on a real `SetupStatus`, and on `"error"` render a minimal retry row instead of silently entering the app: `catch(() => setSetup("error"))` + `fetchSetupStatus().then(setSetup).catch(() => setSetup("error"))` with a retry button.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic+security-10.6 — Filtered recommendation list can be empty with no empty-state (toolbar + blank grid)
- **File**: `src/ui/App.tsx:93`
- **Rilevato da**: logic+security · **Verificato da**: verifier indipendente (batch src/ui/App.tsx)
- **Severità**: LOW
- **Descrizione/evidenza**: `if (gemsOnly) list = list.filter(...); if (format !== "all") list = list.filter(...)` — the `emptyState` message keys only on the unfiltered array: `{result && result.recos.length === 0 && <p className="empty">…</p>}` (line 152), while the grid renders under `result && result.recos.length > 0` (line 154). With `gemsOnly` checked plus a `format` whose only entries are not gems, `recos` is empty but the toolbar (`<h2>{tr(lang,"recommendations")}</h2>`, line 157) and an empty `<div className="grid">` render with no feedback.
- **Verifica**: Verificato alla lettera: emptyState è chiato sul array non filtrato (App.tsx:152 result.recos.length === 0), toolbar+grid renderizzano sotto result.recos.length > 0 (linea 154) e la lista renderizzata è quella filtrata da gemsOnly/format (linee 93-94). Quindi con gemsOnly + un format i cui item non sono gem, recos=[] ma result.recos>0: toolbar e <div className="grid"> vuoto senza nessun feedback. Difetto reale ma puramente cosmetico/locale, richiede una combinazione specifica di filtri impostati…
- **Fix suggerito**: Base the empty message on the filtered list: `{result && recos.length === 0 && <p className="empty">{tr(lang, "emptyState")}</p>}`, optionally with a 'reset filters' link (`setGemsOnly(false); setFormat("all")`).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic+security-10.7 — 1.5s poll can overwrite a fresher status written by finish() (no response-ordering guard)
- **File**: `src/ui/components/SetupWizard.tsx:27`
- **Rilevato da**: logic+security · **Verificato da**: verifier indipendente (batch src/ui/components/SetupWizard.tsx)
- **Severità**: LOW
- **Descrizione/evidenza**: The interval (`setInterval(() => { fetchSetupStatus().then(setStatus).catch(() => undefined); }, 1500)`) and `finish`'s own `const s = await fetchSetupStatus(); setStatus(s);` (lines 46-47) race with no ordering guard: a poll request issued before `postSetup("finish")` completed can resolve after `finish`'s refresh and overwrite the fresh status with a pre-action snapshot, so `step` flickers back (3 -> 1/2) for up to one poll interval.
- **Verifica**: Meccanismo reale e verificato: setInterval(1500) con .then(setStatus) senza guardia di ordinamento, in corsa con il setStatus di finish. La finestra non e' puramente teorica: /status esegue httpOk (1s) + downloadedModels con runLms ls --json (timeout 15s) + resolveLms che puo' fare spawnSync 'lms --version', quindi una poll in flight al momento del click e' plausibile. Conseguenza pero' limitata: flicker di step 3->1 per al massimo un intervallo di poll, auto-corretto (la config e' gia' committe…
- **Fix suggerito**: Track a monotonically increasing request id (or a `busyRef`) and drop stale responses: `const req = ++reqId.current; fetchSetupStatus().then(s => { if (req === reqId.current) setStatus(s); })` — or simply skip poll updates while `busy` is true.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### performance+logic-6.10 — Explanation cache writes are non-atomic while the rest of the app uses tmp+rename: concurrent /explain can interleave on the same key file
- **File**: `src/server/llm.ts:89`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/llm.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `await writeFile(join(EXPL_DIR, `${key}.json`), JSON.stringify({ exp: ..., items }));` writes the final path directly with no temp+rename, unlike the atomic pattern already in the codebase (config.ts updateConfig: `writeFileSync(`${path}.tmp`, ...); renameSync(...)`). Two concurrent /api/explain requests that miss the same subset key (e.g. after the 10-min resultCache expiry recomputes a new id set, or two tabs) open the same file with truncating 'w' concurrently; interleaved writes yield a corrupt file. Impact is bounded (cacheGet's JSON.parse then throws -> treated as a miss) but it silently…
- **Verifica**: Fatto verificato: cacheSet (llm.ts:89-92) scrive direttamente il path finale, mentre il resto del codebase usa tmp+rename (config.ts:47-48, commento 'Atomic write'). La concorrenza è raggiungibile: nessun lock o serializzazione su /explain (unico lock del server è setup.ts:235, solo wizard), Hono/node-server gestisce le request in parallelo e llmChat attende fino a 30s, quindi due tab/doppio submit sulla stessa chiave si sovrappongono; il resultCache di recommend.ts (10 min) rende il fix plausib…
- **Fix suggerito**: Reuse the existing pattern: `await writeFile(`${path}.tmp`, ...)` then `await rename(...)` (node:fs/promises rename) inside cacheSet.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### performance+logic-6.3 — top-5 community seed includes negatively-sentimented entries, so recommendations of shows the user hates get a score bonus
- **File**: `src/server/recommend.ts:82`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/recommend.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const top5 = [...entries].map((e) => ({ e, s: entrySentiment(e, profile.meanScore).s })).sort((a, b) => b.s - a.s).slice(0, 5);` has no `s > 0` filter. entrySentiment (profile.ts:31-41) clamps to [-1, 1] with DROPPED base -0.6 (`statusBase: { ... DROPPED: -0.6 }`, config.ts), so a user with fewer than 5 non-negative entries fills the remaining slots with their most-hated shows, and `fetchRecommendations` of those feeds `community.set(targetId, Math.min(WEIGHTS.communityCap, ... + WEIGHTS.communityPerHit))` (recommend.ts:87-90) — up to +0.1 on a 0..1.1 final score (≈9 display points) for candi…
- **Verifica**: Core claim verified: top5 (recommend.ts:79-82) has no `s > 0` filter. Repro with real entrySentiment: a list with 2 positives + 4 drops seeds 3 DROPPED shows (s=-0.6, statusBase in config.ts:88), whose recommendation graphs then earn community bonuses capped at +0.1 (communityPerHit 0.03/communityCap 0.1) on a 0..1.1 final — i.e. candidates similar to hated shows get up to ~9/110 points of unearned boost. Downgraded to LOW because the trigger requires fewer than 5 non-negative entries (light/dro…
- **Fix suggerito**: Filter the seed list before slicing: `.filter(({s}) => s > 0).sort(...).slice(0, 5)` (keep the sort before the filter if you want the strongest positives first — order the filter first, then sort desc, then slice).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### performance+logic-6.4 — missingEntries passed unchunked to fetchMediaByIds: AniList Page(perPage:50) silently truncates, those franchises vanish from results
- **File**: `src/server/recommend.ts:61`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/recommend.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const extra = await fetchMediaByIds(missingEntries);` passes an unbounded array into `MEDIA_BY_IDS_QUERY` which is hard-coded to `Page(perPage: 50)` (anilist.ts:66) with no pagination loop in fetchMediaByIds (anilist.ts:290-301). For every id beyond the first 50 matches, AniList just omits it. The failure is silent and asymmetric: the corresponding sequel was already removed at recommend.ts:64 (`candidates0.filter((c) => !superseded.has(c.id) ...)`), so with >50 missing entry points the whole franchise disappears from the recommendations instead of being represented by its entry point. candid…
- **Verifica**: Mechanism confirmed from code: MEDIA_BY_IDS_QUERY hard-codes Page(perPage: 50) (anilist.ts:66) and fetchMediaByIds (anilist.ts:290-302) has no pagination/chunking, while recommend.ts:61 passes the unbounded missingEntries array; AniList silently omits matches beyond the first 50. The loss is asymmetric because the sequel was already removed at recommend.ts:64, so the chain vanishes instead of being represented by its entry point. Downgraded from MEDIUM: triggering needs >50 distinct entry points…
- **Fix suggerito**: Chunk in recommendFor: `const extra = (await Promise.all(chunk(missingEntries, 50).map((ids) => fetchMediaByIds(ids)))).flat();` or add the chunking inside fetchMediaByIds next to the existing `if (ids.length === 0) return [];` guard so every caller is protected.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### performance+logic-6.5 — fetchMediaByIds is the only unguarded network step in the orchestration: one failure after retries fails the whole recommendation
- **File**: `src/server/recommend.ts:61`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/recommend.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: Every other AniList step degrades: candidates.ts:45 `fetchMediaPage(q).catch(() => ({ media: [], hasNextPage: false }))` and recommend.ts:83 `fetchRecommendations(e.mediaId).catch(() => [])`. Line 61 `const extra = await fetchMediaByIds(missingEntries);` has no catch, so a single exhausted-retry AniListError (502, anilist.ts `throw new AniListError(...)` after `attempt < 3`) propagates out of recommendFor and turns the entire /api/recommend request into a 502 (api.ts:53 errorResponse) even though a complete candidate pool and scoring already exist without the entry-point cards.
- **Verifica**: Confirmed: recommend.ts:61 `await fetchMediaByIds(missingEntries)` is the only optional network step without a catch — every sibling degrades (candidates.ts:39 `.catch(() => ({ media: [], hasNextPage: false }))`, recommend.ts:83 `.catch(() => [])`). An AniListError after exhausted retries (anilist.ts:136/150) propagates out of recommendFor to api.ts errorResponse as a 502, discarding an already-complete pool. LOW is right: it requires exhausted retries or a GraphQL-level error on that one query …
- **Fix suggerito**: Match the surrounding posture: `const extra = await fetchMediaByIds(missingEntries).catch(() => []);` — the entry-point badge is then silently omitted instead of killing the request; optionally log the failure.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### performance+logic-6.6 — Dead branch: franchise.get(orig) is always undefined, so the entry-point/sequel rootId stand-in never runs and the second dedupeFranchises is a no-op
- **File**: `src/server/recommend.ts:105`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/recommend.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const orig = epMap.get(r.media.id); const origInfo = orig != null ? franchise.get(orig) : undefined; if (origInfo) r.rootId = origInfo.rootId;` can never fire: every epMap value is in `superseded` (line 62) and `franchise` is built only from `candidates`, which excludes `superseded` (line 64 `candidates0.filter((c) => !superseded.has(c.id) ...)`), and epMap keys can never equal values (line 51 guards `f.entryPointId === id`), so extras (line 61) never reintroduce an orig id. Reproduced with the real analyzeFranchises on pool [S2(prequel S1), S3(prequel S2)]: `epMap [[1,3]]`, `franchise.get(3)…
- **Verifica**: Reproduced with the real analyzeFranchises on pool [S2(prequel 1), S3(prequel 2)]: epMap=[[1,3]], franchise built from candidates=[2,1] (superseded 3 removed at line 64, and epMap keys can never equal values per the line-51 guard), so franchise.get(3) is undefined and the relink loop hit 0 times — the intent documented at line 102 never executes. Also verified the mitigating fact: rootIds already coincide (entry point root=1 equals the sequel's would-be chain[0]=1), so dedupe output is unchanged…
- **Fix suggerito**: Either delete lines 103-107 and the second dedupeFranchises at line 108 (rootIds already coincide: an entry point's own `chain[0] ?? m.id` is the chain root the sequel would report), or make the intent real by folding the superseded chain into the count before dedupe — e.g. seed `sizeByRoot` with epMap entries (`epMap.size` per root) and drop the redundant second call.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### performance+logic-6.7 — No single-flight on resultCache: concurrent /recommend or /explain for the same user each run the full pipeline (AniList + LLM)
- **File**: `src/server/recommend.ts:26`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/recommend.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const hit = resultCache.get(cacheKey); if (hit && Date.now() - hit.at < RESULT_TTL_MS) return hit.result;` then `await recommendFor(...)` then `resultCache.set(...)` — check-then-compute with no in-flight promise sharing. Two overlapping requests for the same username (double click, two tabs, or /api/explain which itself calls getRecommendation at api.ts:58 while a /recommend is running) both execute the ~13-request candidate fan-out plus the LLM call, duplicating cost/latency and racing on `resultCache.set` (last writer wins). AniList's token bucket (anilist.ts takeToken) throttles but does …
- **Verifica**: Confirmed by reading getRecommendation (recommend.ts:24-31): cache read, then `await recommendFor(...)`, then set — no in-flight promise map anywhere (grep confirms resultCache is only lines 10/26/30). Two overlapping requests for the same username:lang (double click, two tabs, or /api/explain which calls getRecommendation at api.ts:58) each run the full ~13-request AniList fan-out plus LLM call; token bucket throttles but does not dedupe. No correctness impact (last-writer-wins of equivalent re…
- **Fix suggerito**: Cache the promise: `let p = inflight.get(cacheKey); if (!p) { p = recommendFor(username, lang, opts).finally(() => inflight.delete(cacheKey)); inflight.set(cacheKey, p); } const result = await p; resultCache.set(cacheKey, { at: Date.now(), result });`
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### performance+logic-6.8 — resultCache is never pruned: TTL is only checked on read, entries for abandoned usernames live forever
- **File**: `src/server/recommend.ts:10`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/recommend.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const resultCache = new Map<string, { at: number; result: RecoResult }>();` has no eviction: expired entries are only skipped at line 27 (`if (hit && Date.now() - hit.at < RESULT_TTL_MS)`), never deleted, and every successful getRecommendation adds an entry (line 30). Each value holds up to 50 MediaLite objects including 500-char descriptions (anilist.ts mapMedia), so scripted or exploratory queries across many usernames grow the map without bound for the process lifetime.
- **Verifica**: Confirmed: resultCache (recommend.ts:10) is never pruned — TTL is only checked on read (line 27), every success inserts (line 30), and grep shows no delete/clear anywhere. Entries hold up to 50 recos + 12 avoided MediaLite objects with 500-char descriptions, so it is a genuine unbounded leak for the process lifetime. LOW because the realistic ceiling is tiny for a self-hosted single-user tool: the key is username:lang, so even scripted exploration across hundreds of users stays in the low tens o…
- **Fix suggerito**: Prune on write: before `resultCache.set(...)`, `for (const [k, v] of resultCache) if (Date.now() - v.at >= RESULT_TTL_MS) resultCache.delete(k);` or cap size (delete oldest when > N entries).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### performance+logic-6.9 — parseExplanations rejects numeric-string ids, silently discarding valid model output
- **File**: `src/server/llm.ts:70`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/llm.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `arr.filter((x) => typeof x?.id === "number" && typeof x?.why === "string")` — verified: input `"[{"id":"1","why":"a"}]"` -> `[]` (the item, and here the whole array, is dropped). Small local models frequently emit `"id": "123"` even when the prompt at llm.ts:58 shows `"id":<media id>` unquoted; those explanations silently fall back to the deterministic why (and via performance+logic-6.1 get locked in cache for 7 days).
- **Verifica**: Riprodotto: parseExplanations('[{"id":"1","why":"a"}]') → []. Il filtro typeof x?.id === 'number' a llm.ts:70 scarta gli id numerici tra virgolette che i modelli piccoli emettono frequentemente anche con prompt non quotato (llm.ts:58); l'item non matcha batch.find a llm.ts:130 e degrada silenziosamente al why deterministico (che via 6.1 resta in cache 7 giorni). Impatto basso e locale: una spiegazione per item, nessun errore, il testo di fallback è comunque valido. Non è mitigato altrove (nessun…
- **Fix suggerito**: Coerce instead of rejecting: `const id = typeof x?.id === "number" ? x.id : Number(x.id); return id !== null && Number.isInteger(id) && typeof x?.why === "string" && ...` before the `batch.find((r) => r.media.id === e.id)` lookup at llm.ts:130.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### security-1.3 — Disclosure non autenticata di configurazione interna e percorsi filesystem (/api/config, /api/setup/status)
- **File**: `src/server/api.ts:21`
- **Rilevato da**: security · **Verificato da**: verifier indipendente (batch src/server/api.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: src/server/api.ts:21 `api.get("/config", (c) => c.json({ llm: { model: llmModel(), baseUrl: llmBaseUrl() } }));` espone l'endpoint LLM configurato (che dopo security-1.1 può essere un host esterno). La route montata `/api/setup/status` (src/server/setup.ts:320-329) restituisce `lms: { installed, path, serverUp }` con il path assoluto del binario (`/Users/<user>/.lmstudio/bin/lms`), fingerprint hardware (`os`, `chip`, `ramGb`, `appleSilicon`), lista modelli scaricati e stato del job. Nessuna autenticazione: informa qualsiasi processo locale non privilegiato (o, via rebinding, un sito remoto) su…
- **Verifica**: Fatti tutti verificati: api.ts:21 `/api/config` restituisce { model, baseUrl } con llmBaseUrl() che dopo security-1.1 può essere un host esterno; /api/setup/status (setup.ts:312-329) restituisce hardware completo (os/chip/ramGb/appleSilicon via detectHardware:34-44), lms.path assoluto (resolveLms:67-81, es. $HOME/.lmstudio/bin/lms), downloadedModels, `job` (incluso logTail con output subprocess, ring buffer setup.ts:162-165) e suggested (repo modelli). Nessuna autenticazione e nessuna mitigazion…
- **Fix suggerito**: Ridurre il payload: non esporre `lms.path` assoluto (basta il booleano `installed`), non esporre ramGb/chip fuori da un flag debug, e non restituire `llm.baseUrl` su /api/config (solo model). Se serve alla UI di setup, gated dallo stesso middleware di security-1.1.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### security+performance-3.10 — cacheWrap has no single-flight: concurrent identical requests duplicate the most expensive fetch and burn rate-limit tokens
- **File**: `src/server/anilist.ts:103`
- **Rilevato da**: security+performance · **Verificato da**: verifier indipendente (batch src/server/anilist.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: anilist.ts:103-115 — on a miss every caller runs `await fn()` independently; the dedup only exists on disk after the write (line 113). Concretely, `/api/profile/:user` and `/api/recommend` issued together (recommend.ts:15 and recommend.ts:39 both call `fetchUserList(username)` with the same cache key from gql at line 120) each miss, each replay up to 22 chunk requests through the 25/min token bucket (anilist.ts:86-97), doubling cold-start latency and consuming half the budget — and under a 429 both loops pile onto the same unbounded retry path. The module-level `tokens` state is correctly shar…
- **Verifica**: Il meccanismo e' vero: cacheWrap (anilist.ts:103-115) non ha single-flight, il dedup esiste solo su disco dopo la write, quindi richieste identiche concorrenti duplicano il fetch e si spartiscono lo stesso token bucket raddoppiando latenza/budget. Pero' lo scenario di impatto citato e' smentito dal codice: App.tsx:62-64 attende fetchProfile PRIMA di fetchRecommend (sequenziale, non concorrente come affermato), /explain riusa resultCache (recommend.ts:10-27) quindi non rifetcha, e la cache disco …
- **Fix suggerito**: Add an in-flight promise map keyed by the same hash: `const inflight = new Map<string, Promise<unknown>>();` — in cacheWrap, `const p = inflight.get(file) ?? fn().finally(() => inflight.delete(file)); inflight.set(file, p); return p as Promise<T>;`
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### security+performance-3.3 — fetchMediaByIds silently drops ids beyond the first 50 (perPage:50 with no pagination)
- **File**: `src/server/anilist.ts:296`
- **Rilevato da**: security+performance · **Verificato da**: verifier indipendente (batch src/server/anilist.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: anilist.ts:67 `Page(perPage: 50) { media(id_in: $id_in, ...) }` — the query has no `page` variable and fetchMediaByIds (anilist.ts:290-302) never loops or checks `pageInfo`. AniList caps Page at 50 items, so with `ids.length > 50` only the first 50 (default id order) come back and the rest vanish with no error. Caller recommend.ts:58-61 passes `missingEntries`, which is derived from `epMap.keys()` (one entry point per candidate franchise over a ~650-item pool) and is unbounded — franchise-heavy lists silently lose entry points, and the later `extra.filter(...)` merge just proceeds with fewer c…
- **Verifica**: Meccanismo certo: MEDIA_BY_IDS_QUERY (anilist.ts:64-69) non ha variabile `page` ne' check su pageInfo, e fetchMediaByIds non chunka; docs/anilist-api.md:21 (fatti verificati della repo) conferma il cap `Page(perPage <= 50)`, quindi ids>50 = dati perso silenziosamente. Magnitude non dimostrabile: ho eseguito analyzeFranchises reale sui fixture registrati e missingEntries=0 (pool di 8: fixture stub registrata durante l'outage), quindi la frequenza con cui si supera 50 su un pool reale da ~650 rest…
- **Fix suggerito**: Chunk the input and concat: `const out: MediaLite[] = []; for (let i = 0; i < ids.length; i += 50) { const d = await gql<...>(MEDIA_BY_IDS_QUERY, { id_in: ids.slice(i, i + 50) }, CACHE_TTL_MEDIA_MS); out.push(...d.Page.media.map(mapMedia)); } return out;`
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### security+performance-3.4 — 5xx/non-JSON response after retries escapes as a raw SyntaxError instead of AniListError
- **File**: `src/server/anilist.ts:147`
- **Rilevato da**: security+performance · **Verificato da**: verifier indipendente (batch src/server/anilist.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: anilist.ts:143-147 — `if (res.status >= 500 && attempt < 3) { ...continue; }` then unconditionally `const json = await res.json()`. On the 4th 5xx (or any 4xx/3xx returning HTML, e.g. a 502 HTML error page from a proxy), `res.json()` rejects with a SyntaxError from outside the `try` block that guards `fetch` (anilist.ts:124-137), so it propagates as a plain SyntaxError with a message like `Unexpected token '<'` instead of an `AniListError` carrying the HTTP status. Callers that inspect/catch `AniListError` see an unrelated failure, and candidates.ts:39's blanket `.catch` masks it entirely (emp…
- **Verifica**: Path verificato: dopo 3 retry 5xx (attempt=3, anilist.ts:143) si cade su `await res.json()` (riga 147) fuori dal try che protegge fetch (124-137): body HTML/da proxy Cloudflare -> SyntaxError che propaga come errore qualunque, errorResponse (api.ts:74) risponde 500 internal_error invece di 502 anilist_error. Il difetto e' reale ma l'impatto e' solo di classificazione/osservabilita' (in entrambi i casi il client riceve un errore; il catch blanket di candidates.ts:39 maschera qualunque tipo comunq…
- **Fix suggerito**: Parse inside a guard and classify: `let json; try { json = await res.json(); } catch { if (attempt < 3) { await sleep(1000 * 2 ** attempt); continue; } throw new AniListError(`AniList HTTP ${res.status} (non-JSON body)`, res.status); }`
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### security+performance-3.7 — record-fixtures.mjs writes incomplete fixtures and exits 0 when recommendation fetches fail
- **File**: `scripts/record-fixtures.mjs:33`
- **Rilevato da**: security+performance · **Verificato da**: verifier indipendente (batch scripts/record-fixtures.mjs)
- **Severità**: LOW
- **Descrizione/evidenza**: record-fixtures.mjs:33 `const recs = await fetchRecommendations(e.mediaId).catch(() => []);` followed by line 34 `if (recs.length > 0) recMap[...]` and line 43's unconditional write + line 44 "fixtures written". A network outage or AniListError during the top-5 loop is indistinguishable from "no recommendations": the script silently produces a recommendations.json with missing/empty entries, exits 0, and every later fixture-mode run (tests/api.test.ts, README offline dev) inherits the degraded data with no hint that recording half-failed.
- **Verifica**: Logica confermata su codice: record-fixtures.mjs `.catch(() => [])` sul loop top-5, scrittura incondizionata dei 3 file e "fixtures written" con exit 0 — un AniListError/timeout/network outage su quelle fetch è indistinguibile da "nessuna raccomandazione" e produce un recMap parziale o `{}`. Non è mitigato a monte da nulla nel percorso dello script (il retry di gql — 3 tentativi + backoff + handling 429 — riduce ma non elimina il caso persistente). Però l'impatto reale è piccolo: raccomandations…
- **Fix suggerito**: Count failures and surface them: `let failed = 0; ... .catch(() => { failed++; return []; });` then before writing: `if (failed) { console.error(`${failed}/5 recommendation fetches failed`); process.exitCode = 1; }` (or retry once before giving up).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### security+performance-3.9 — mapMedia guards every nullable field except title, so one bad entry aborts the whole list fetch
- **File**: `src/server/anilist.ts:179`
- **Rilevato da**: security+performance · **Verificato da**: verifier indipendente (batch src/server/anilist.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: anilist.ts:179 `title: m.title.romaji` is the only unguarded dereference in the mapper, while its siblings are all defensively handled: `m.genres ?? []` (182), `m.tags ?? []` (183), `m.studios?.nodes?.[0]?.name ?? null` (186), `m.popularity ?? 0` (188), `m.coverImage?.large ?? null` (189). AniList's schema types Media.title as nullable even though RawMedia (anilist.ts:162) claims otherwise; a single such entry in the 11k-entry list makes mapMedia throw inside fetchUserList's loop, outside any try, killing the entire `/api/profile` request for one record.
- **Verifica**: Incoerenza reale e puntuale: anilist.ts:179 `m.title.romaji` e' l'unico deref non difensivo del mapper (tutti gli altri campi nullable sono guardati a righe 182-189), mentre Media.title e' nullable nello schema AniList quindi il tipo RawMedia (riga 162) bugiana. Se title fosse null, mapMedia lancia dentro il loop di fetchUserList e abortisce l'intero /api/profile. Oggi pero' non risulta alcun media AniList con title null (caso limite teorico dello schema) e il sotto-caso `romaji: null` non crash…
- **Fix suggerito**: `title: m.title?.romaji ?? m.title?.english ?? `(id ${m.id})`,` — cheap and consistent with the rest of the mapper (downstream already tolerates null-ish titles via ListEntry.title consumers).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-8.1 — studio/era affinity (20% of the core weight) never asserted: rows execute, output never distinguished
- **File**: `tests/scoring.test.ts:25`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/scoring.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: The profile fixture only carries tag+genre dims: `loved: [{ dim: "tag", value: "Psychological", ... }, { dim: "genre", value: "Mystery", ... }], disliked: [{ dim: "genre", value: "Isekai", ... }]` (scoring.test.ts:31-35). WEIGHTS.studio=0.12 and WEIGHTS.era=0.08 (src/server/config.ts:69-70) — together 20% of the affinity core — are never exercised with a non-zero value anywhere in the two files, so scoring.ts:36-37 (`const studioScore = aff("studio", m.studio); const eraScore = aff("era", ...")`) always evaluates to 0 in tests. Notably scoring.ts:37 re-implements bucketing inline — `aff("era",…
- **Verifica**: Mutation-proven end-to-end: (a) replacing the inline era bucketing at scoring.ts:37 with a wrong non-bucketed key and (b) zeroing WEIGHTS.studio/era (config.ts:68-69) both leave the FULL 21-test suite green — no test anywhere puts a studio/era dim in a profile fixture, and affinityOf().parts is never asserted (grep confirms). The scoring.ts:37 duplication of eraBucket (profile.ts:32) is a real drift hazard with zero test backstop, and the api.test.ts fixture flow does not catch it either. Downgr…
- **Fix suggerito**: Add a case with studio+era dims in the profile fixture: build a profile with `loved: [{dim:"studio", value:"Madhouse", aff:0.6, support:5, examples:["X"]}, {dim:"era", value:"2015", aff:0.5, support:5, examples:["X"]}]` and assert `affinityOf(media(1, {studio:"Madhouse", seasonYear:2017}), p).parts` has `studio > 0` and `era > 0` (2017 must bucket to "2015"). Best: change scoring.ts:37 to call `er
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-8.2 — entrySentiment clamp bounds never exercised: only interior values are asserted
- **File**: `tests/profile.test.ts:44`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/profile.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: All asserted sentiments are interior to the clamp: `(90-80)/40 = 0.25` (line 52 `assert.equal(entrySentiment(entry(4, "COMPLETED", 90), mean).s, 0.25)`), repeat bonuses 0.45/0.55 (lines 54-55), DROPPED base -0.6 (line 57). src/server/profile.ts:25 `clamp((e.score - mean) / WEIGHTS.scoreSpread, -1, 1)` and :27 `clamp(base + sRaw + repeatBonus, -1, 1)` are never evaluated at or beyond ±1 (e.g. score 100 with mean 60 → exactly 1.0; score 20 → -1.0). Deleting or loosening either clamp (e.g. to ±2) passes the whole suite, so the guarantee that sentiment stays in [-1,1] — which feeds `aff` in DimVal…
- **Verifica**: Mutation-proven on the real repo. All asserted sentiments in tests/profile.test.ts:44-60 are interior ((90-80)/40=0.25, 0.45, 0.55, -0.6); no test anywhere asserts the ±1 bound. Empirical results (full suite `node --test tests/*.test.ts`, baseline 21/21): (a) loosening the outer clamp src/server/profile.ts:27 to ±2 → 21/21 pass; (b) deleting the inner clamp line 25 entirely → 21/21 pass; (c) deleting the outer clamp line 27 entirely → also 21/21 pass (the single api.test.ts failure seen once was…
- **Fix suggerito**: Add boundary cases: `assert.equal(entrySentiment(entry(4,"COMPLETED",100), 60).s, 1)` (sRaw exactly at bound) and an over-range case such as `entrySentiment(entry(4,"COMPLETED",100,3), 30)` where base+sRaw+repeat exceeds 1, asserting the total clamps to 1 (and symmetric -1 for a low score with DROPPED base).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-8.6 — isGem thresholds tested only far from boundaries: off-by-one at gemMaxPopularity/gemMinScore invisible
- **File**: `tests/scoring.test.ts:62`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/scoring.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: The gem test uses popularity 5000 vs 500000 and averageScore 78 vs 60 (lines 63-68, 73) — nowhere near the gates in src/server/scoring.ts:61-64 `m.popularity < WEIGHTS.gemMaxPopularity` (40_000) and `(m.averageScore ?? 0) >= WEIGHTS.gemMinScore` (72). Values like popularity 39_999 vs 40_000, score 71 vs 72, and a gemScore straddling gemMinGemScore=0.45 are never asserted, so a regression from `<` to `<=` or `>=` to `>` on any of the three gates passes the suite.
- **Verifica**: Confirmed by mutation: flipping `m.popularity < gemMaxPopularity` to `<=`, `(m.averageScore ?? 0) >= gemMinScore` to `>`, and `gem >= gemMinGemScore` to `>` each passes the suite, since the gem test only exercises popularity 5000/500000 and score 78/60, far from the 40_000 / 72 / 0.45 gates (scoring.ts:61-65). Real coverage hole, but the exposed regression is an off-by-one at exactly one boundary value with negligible user impact — LOW.
- **Fix suggerito**: Add boundary assertions using the same aff computed at lines 69-70: `assert.ok(!isGem(media(1,{popularity:40000, averageScore:78, ...}), gemScore))` vs `isGem(media(1,{popularity:39999, ...}))`, and one pair for averageScore 71/72.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-8.7 — Vacuous / one-sided assertions: object truthiness, bare length check, single adjacent pair for sortedness
- **File**: `tests/scoring.test.ts:188`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/scoring.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: Three symptoms of the same weak-assertion habit: (1) line 188 `assert.ok(info.get(501));` — a Map lookup returning an object is truthy by construction, and the cycle test asserts no `kind`/`entryPointId` for either member, so any classification is accepted as long as nothing hangs; (2) line 97 `assert.ok(top.why.length > 10)` — any string over 10 chars passes, including a template that emitted a placeholder or an error message; (3) line 98 `assert.ok(recos[0].final >= recos[1].final)` — checks one adjacent pair of an array the test name claims is "sorted by final", so a partially-broken sort (…
- **Verifica**: All three symptoms verified: (1) `assert.ok(info.get(501))` is vacuous because analyzeFranchises sets info unconditionally for every candidate (franchise.ts:68), so any cycle classification passes — and a concrete one exists (actual behavior for the 501<->502 cycle with empty list is ENTRY_POINT/entryPointId 502, so the suggested assert is implementable); (2) `top.why.length > 10` accepts any string; (3) mutation-proven: replacing the comparator with `sort(() => 0)` still passes the test named '…
- **Fix suggerito**: (1) assert concrete classification for the cycle, e.g. `assert.equal(info.get(501)?.kind, "ENTRY_POINT")` plus entryPointId; (2) assert content: `assert.ok(top.why.includes("Psychological"), top.why)` (the candidate has the loved tag); (3) assert full ordering: `assert.deepEqual(recos.map(r=>r.final), [...recos.map(r=>r.final)].sort((a,b)=>b-a))`.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-8.8 — dedupeFranchises test manufactures its scenario by mutating output, and covers a single franchise root
- **File**: `tests/scoring.test.ts:114`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/scoring.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: The test overrides the function's own output — `// force media(2) to win the franchise` `recos[1].final = 0.99;` `recos.sort((a, b) => b.final - a.final);` — which relies on V8 stable-sort tie-breaking (media(1) and media(2) have identical metadata, affinity and NEXT_STEP bonus, so their pre-mutation order is arbitrary) for `recos[1]` to be media(2). It also contains exactly one franchise root (rootId 100), so the multi-franchise contract — one representative per *distinct* root, groupSize computed per root, kept items preserving input order across several groups — is never exercised.
- **Verifica**: Core criticism holds: the test manufactures its winner by mutating output (recos[1].final = 0.99 at scoring.test.ts:114) instead of constructing inputs, and covers a single franchise root, so the multi-root contract (one representative per DISTINCT root, independent per-root groupSize, input-order preservation across groups) is never exercised — a grouping regression crossing roots would pass. One sub-claim is overstated: `recos[1] === media(2)` is NOT arbitrary — media(1) and media(2) have iden…
- **Fix suggerito**: Construct the ranking from inputs instead: give media(2) a higher averageScore (or a loved tag overlap) so it wins naturally, and add a second franchise (rootId 200 with 2 members) plus an EXCLUDED member to assert `deduped.length` and each `groupSize` across three roots. Optionally keep a separate explicit test that ties inside a group are resolved deterministically.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-8.9 — scoreAll badge/bonus coupling untested: only NEXT_STEP path asserted, ENTRY_POINT/SPIN_OFF/HIDDEN_GEM outputs never checked
- **File**: `tests/scoring.test.ts:96`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/scoring.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: The only scoreAll badge assertion is `assert.ok(top.badges.includes("NEXT_STEP"))` (line 96). src/server/scoring.ts:165-176 gates the franchise bonus on kind only for NEXT_STEP (`if (f?.kind === "NEXT_STEP") badges.push("NEXT_STEP")`, `(badges.includes("NEXT_STEP") ? WEIGHTS.franchiseBonus : 0)`) and also emits ENTRY_POINT, SPIN_OFF (`isSpinOff`) and HIDDEN_GEM badges (isGem is only tested directly at :71-74, never through scoreAll). A regression that grants franchiseBonus to ENTRY_POINT (inflating entries the user cannot yet watch) or drops the HIDDEN_GEM assignment inside scoreAll passes the…
- **Verifica**: Partially refuted by full-suite mutation testing; survives on a narrower kernel. REFUTED: 'drops the HIDDEN_GEM assignment inside scoreAll passes the whole suite' is false (removing scoring.ts:170 fails api.test.ts:70) and 'ENTRY_POINT ... never checked' is false at suite level (removing scoring.ts:167 fails api.test.ts:66) — those two badges are covered end-to-end by the fixture API smoke test, not by scoring.test.ts. SURVIVES: granting the +0.12 franchiseBonus to ENTRY_POINT passes the entire …
- **Fix suggerito**: Extend the scoreAll fixture with an ENTRY_POINT candidate and a spin-off, then assert: ENTRY_POINT is badged but `breakdown`-derived final lacks the +0.12 bonus (compare against a NEXT_STEP twin with equal metadata), `SPIN_OFF` appears, and a gem-qualified candidate gets `HIDDEN_GEM` in `scoreAll(...)` output.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-9.1 — Assertion tautologica su health.llm.state: verifica solo che il valore appartenga all'unione dei suoi stessi valori possibili
- **File**: `tests/setup.test.ts:135`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/setup.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `assert.ok(["up", "starting", "off"].includes(health.llm.state));` — `llmBackendState()` (src/server/setup.ts:222 `export type BackendState = "up" | "starting" | "off"`) può restituire solo uno di quei tre valori, quindi l'assert non può fallire se non per violazione del tipo. Il test dichiara di verificare lo stato LLM dopo `/api/setup/finish` (riga 134) ma qualunque comportamento (backend mai partito, stato mai aggiornato, `ensureLlmServer` non invocato affatto) passa. Coverage teatrale: la riga viene eseguita, nessuna proprietà è verificata.
- **Verifica**: Confermato: health.llm.state viene da llmBackendState() (setup.ts:226-228) che ritorna la module-var backendState, tipizzata esattamente all'unione testata (setup.ts:222), quindi l'assert a tests/setup.test.ts:135 non può fallire per alcun comportamento di ensureLlmServer/run() — solo un rename/rimozione del campo lo farebbe fallire (nicchia rispetto alla claim 'solo violazione di tipo', ma la sostanza regge). Aggravante non citata: al momento del fetch (riga 134) run() è ancora nel loop di poll…
- **Fix suggerito**: Verificare il valore deterministico nell'ambiente isolato: con il fake lms che non espone HTTP, run() termina con `backendState = "off"` (setup.ts:283), quindi `assert.equal(health.llm.state, "off")`; in alternativa iniettare LMSTUDIO_BASE e attendere polling fino allo stato atteso.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-9.3 — Il fake LLM verifica nessuna parte del contratto di richiesta: prompt, lingua e profilo gusto possono regredire senza far fallire il test
- **File**: `tests/llm.test.ts:76`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/llm.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: L'handler del server finto usa il body solo per estrarre gli id: `const ids = [...body.messages.at(-1).content.matchAll(/id=(\d+)/g)]...` e restituisce sempre `llm says ${id}`. Nessuna assert su `body.messages[0].role`, sul campo `model` inviato da llmChat (src/server/llm.ts:22), né sul contenuto del prompt: se `buildPrompt` smettesse di includere i tag amati (`Psychological`) o `LANG_NAME[lang]` restituisse undefined (il test chiama con `"en"` a riga 84 e `"it"` a riga 101 senza mai controllare che compaia "English"/"Italian"), tutti i test passano comunque. Il test copre il plumbing, non il …
- **Verifica**: Confermato con mutation testing su copia monouso del repo (/tmp, repo originale intatto). L'handler del fake (tests/llm.test.ts:75-80) usa body solo per estrarre gli id e le assert (righe 85-93, 101-103) non ispezionano mai la richiesta. Risultati misurati: (M1) prompt senza profilo gusto ('they love: nothing.' al posto di ${loved||...}) → 2 pass/0 fail; (M2) LANG_NAME senza chiave 'en' → prompt 'in undefined' → 2 pass/0 fail; (M3) llmChat invia model:'totally-wrong-model' → 2 pass/0 fail; (M4) …
- **Fix suggerito**: Aggiungere nel fake 2-3 assert sul body ricevuto, ad es. `assert.ok(user.includes("English")); assert.ok(user.includes("Psychological")); assert.equal(body.model, llmModel())` — contratto di richiesta, non testo esatto del prompt.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-9.4 — Assert presence-only su una sequenza: il test intitolato "ensure sequences lms commands" non verifica alcun ordine
- **File**: `tests/setup.test.ts:129`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/setup.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: Le assert sono tutte `calls.includes(...)`: `"daemon up"`, `"server start"`, `"get prism-ml/Bonsai-27B-gguf --gguf"`, `"--context-length=8192"` — il log è un append indifferenziato (`echo "$0 $*" >> ...`, riga 62), quindi una regressione che riordina i comandi in run() (setup.ts:260-278, dove l'ordine daemon→server→ls→get→load è load-bearing: `server start` prima di `get` servirebbe un modello sbagliato) continua a passare. Inoltre il fake risponde sempre `[]` a `ls`, quindi il ramo "modello già presente → salta download" (setup.ts:269-277) non è mai esercitato e l'assert sul `get` è vera per …
- **Verifica**: Confermato con una correzione al finder. Verificato: log append indifferenziato 'echo "$0 $*" >> file' (test riga 62) e assert tutte substring includes (righe 129-132) → una regressione che riordini daemon/server/ls/get/load in run() (setup.ts:260-278) continua a passare; il ramo skip-download (setup.ts:269-277) non è mai esercitato perché il fake ls risponde sempre '[]' → get è sempre invocato. Correzione: l'assert sul get NON è 'vera per costruzione' nel senso pieno — pina gli argomenti esatti…
- **Fix suggerito**: Loggare una riga per invocazione e fare assert sull'array ordinato, es. `assert.deepEqual(seq(), ["daemon up", "server start", "ls --json", "get prism-ml/Bonsai-27B-gguf --gguf", "load prism-ml/Bonsai-27B-gguf -y --gpu=max --context-length=8192"])`; aggiungere un secondo scenario con fake `ls` che restituisce il modello per coprire il ramo skip-download.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-9.5 — Porte fisse + child kill senza attendere l'exit e stdio ignorato: fallimenti mascherati e porta orfana che rompe il run successivo
- **File**: `tests/api.test.ts:95`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/api.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `const PORT = 4790` (riga 6, analogo `4791` in setup.test.ts:67) è hardcoded: se occupato da un processo precedente sopravvissuto, `child.kill("SIGTERM")` nel finally (riga 95, senza `await new Promise(r => child.on("exit", r))`) può lasciare il figlio a morire dopo la fine del test. Con `stdio: "ignore"` (riga 32) l'errore reale del bind è buttato via e il fallimento si manifesta come il generico `throw new Error("server did not start")` (riga 20) dopo 15s di polling — diagnosi impossibile dal log CI.
- **Verifica**: Confermato empiricamente ma solo in parte. (a) CONFERMATO — failure masking: con la porta 4790 occupata, il run fallisce exit 1 dopo ~15s con solo `error: 'server did not start'` e zero diagnostica (l'EADDRINUSE del figlio finisce nello stderr scartato da stdio:"ignore", riga 32; figlio senza handler di segnale in src/server/index.ts). (b) NEGATO dalla misura — l'orfano che "rompe il run successivo" non si materializza: child.kill("SIGTERM") senza await exit termina il figlio in ~11ms e la porta…
- **Fix suggerito**: Nel finally: `child.kill("SIGTERM"); await once(child, "exit")` (con timeout), cambiare `stdio: "ignore"` in `stdio: ["ignore", "pipe", "pipe"]` includendo stderr nel messaggio di timeout, e allocare la porta con un bind su :0 prima dello spawn invece di costanti fisse.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-9.6 — Cache disco condivisa con il runtime reale, aggirata con Math.random invece che isolata
- **File**: `tests/llm.test.ts:73`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/llm.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: Commento autoriale: `// unique profile hash per run → fresh disk cache key` seguito da `profile.hash = \`h-${Math.random().toString(36).slice(2)}\``. La cache non è isolabile: `CACHE_DIR` è costante di modulo (src/server/config.ts:59) e `explainRecos` scrive in `<repo>/data/cache/expl/<sha>.json` con TTL 7 giorni (llm.ts:88-92): ogni run lascia un file spazzatura nella data dir reale del dev (data/cache esiste già, e data/llm.log ha già 3.5KB di output dei test via setup.test.ts). La correttezza del test dipende dall'unicità di un rand senza seed: se il hash collidesse, il primo `cacheGet` tro…
- **Verifica**: Verificato su disco: CACHE_DIR è costante di modulo senza override (src/server/config.ts:59, new URL('../../data/cache/')), EXPL_DIR derivata (llm.ts:7), cacheSet scrive lì (llm.ts:87-93, TTL 7d config.ts:62). Prima del mio run data/cache/expl conteneva già 16 file con payload letterale dei test ({"1":"llm says 1","2":"deterministic why for 2"} — stringa prodotta solo dal fake di tests/llm.test.ts:78); dopo un run della suite: 19. data/llm.log (gitignored ma file reale) accumula l'output del fak…
- **Fix suggerito**: Rendere CACHE_DIR iniettabile (`process.env.CACHE_DIR ?? ...` in config.ts), puntare i test a `mkdtempSync(join(tmpdir(), "alr-cache-"))` con cleanup nel finally ed eliminare il random: il test resta deterministico e non tocca lo stato reale.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-9.7 — Mutazione di process.env senza restore: LLM_BASE_URL leak tra test e verso consumatori in-process
- **File**: `tests/llm.test.ts:100`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/llm.test.ts)
- **Severità**: LOW
- **Descrizione/evidenza**: `process.env.LLM_BASE_URL = url;` (riga 82) e `process.env.LLM_BASE_URL = "http://127.0.0.1:59999/v1";` (riga 100) non sono mai ripristinati. Oggi è innocuo (node --test isola per file), ma con `node --test --experimental-test-isolation=none` — o qualunque esecuzione single-process — setup.test.ts spawnerebbe il server con `...process.env` (riga 72) ereditando il fake URL: `hasCustomEnv()` (config.ts:56) diventerebbe true e `ensureLlmServer()` (setup.ts:234) farebbe early-return, cosicché l'intera sequenza lms non partirà e il test fallirebbe in funzione dell'ordine dei file, non del codice.
- **Verifica**: Confermato end-to-end, catena esatta come descritto: process.env.LLM_BASE_URL impostato a tests/llm.test.ts:82 e :100 senza mai delete; setup.test.ts:72 propaga ...process.env al server spawnato; src/server/index.ts:26 chiama ensureLlmServer che a src/server/setup.ts:234 fa early-return su hasCustomEnv() (config.ts:56) → sequenza lms mai avviata. Prove: (1) node --test --experimental-test-isolation=none tests/*.test.ts → 20 pass, 1 fail: esattamente il test #21 setup flow, errore 'expected daemo…
- **Fix suggerito**: Salvare/ripristinare in try/finally: `const prev = process.env.LLM_BASE_URL; try { ... } finally { prev === undefined ? delete process.env.LLM_BASE_URL : process.env.LLM_BASE_URL = prev; }`.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

### INFO

#### logic-2.9 — MODEL_KEY_RE allows a leading dash (and ".."), enabling CLI flag injection into lms args
- **File**: `src/server/setup.ts:167`
- **Rilevato da**: security+logic · **Verificato da**: verifier indipendente (batch src/server/setup.ts)
- **Severità**: INFO
- **Descrizione/evidenza**: `const MODEL_KEY_RE = /^[A-Za-z0-9._/-]+$/;` accepts "-y", "--gpu=off" or "../etc/x". Those values flow straight into spawn args: `runLms(lms, ["get", model, flag], ...)` (line 177) and `runLms(lms, ["load", cfg.model!, "-y", "--gpu=max", ...])` (line 278) — no shell, so no code execution, but the value can be parsed by lms as options rather than a key, and "/" plus ".." let a crafted key walk out of the model namespace in lms's own path handling.
- **Verifica**: Verificato: la regex accetta '-y', '--gpu' (senza '=') e '../etc/x' (test eseguito), e i valori fluiscono in spawn args a 177 e 278. Ma niente shell → no code execution; il server è bindato su 127.0.0.1 mono-utente, quindi l'unico 'attaccante' è l'utente locale che potrebbe lanciare lms direttamente; esito peggiore = confusione di flag dentro lms (errore o conferma saltata), nessuna scrittura file da parte nostra. La parte su path-walk in lms è esterna e non dimostrabile. Nit di hardening senza …
- **Fix suggerito**: Require the key to start with an alphanumeric: `/^[A-Za-z0-9][A-Za-z0-9._/-]*$/` (and reject a "/.." segment if the HF org/model shape matters, e.g. `/^[A-Za-z0-9][\w.-]*\/[\w.-]+$/`).
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### logic+security-10.8 — gemRank sentinel -1 is only correct because of a remote config invariant (popularity cap 40k, affinity in [0,1])
- **File**: `src/ui/App.tsx:13`
- **Rilevato da**: logic+security · **Verificato da**: verifier indipendente (batch src/ui/App.tsx)
- **Severità**: INFO
- **Descrizione/evidenza**: `gemRank = r.badges.includes("HIDDEN_GEM") ? r.breakdown.affinity - r.media.popularity / 1_000_000 : -1;` — today gems always rank above every non-gem only because `breakdown.affinity` is `affinity01 ∈ [0,1]` (src/server/scoring.ts:45 `(core + 1) / 2` with weights summing to 1, src/server/config.ts:66-72) and gems require `m.popularity < WEIGHTS.gemMaxPopularity` = 40_000 (src/server/scoring.ts:62, config.ts:83), so gemRank > -0.04 > -1. Nothing in this file encodes that: if `gemMaxPopularity` is ever raised toward the popularity scale the formula itself assumes (÷1_000_000), gems would silent…
- **Verifica**: L'osservazione regge: il sentinella -1 non è strutturalmente fuori dominio e la sua correttezza dipende da invarianti definiti in src/server/config.ts (gemMaxPopularity=40000, pesi che sommano a 1). Nota: la premessa del finding è imprecisa — aff NON è clampato a [-1,1] (profile.ts: s = base + sRaw + repeatBonus con base ∈ [-0.6,0.15], sRaw ∈ [-1,1], repeatBonus ∈ [0,0.3] → s ∈ ~[-1.6,1.45]; aff = media pesata × shrink ≤ 1, nessun clamp in profile.ts:64), quindi affinity01 = (core+1)/2 ∈ ~[-0.3,…
- **Fix suggerito**: Make the sentinel structurally outside the value domain: `: Number.NEGATIVE_INFINITY` (or sort gems first with a boolean comparator), so the ordering cannot depend on `gemMaxPopularity`/weight values defined in src/server/config.ts.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### performance+logic-6.11 — Community-signal fetches fail silently with no logging, invisibly degrading scores
- **File**: `src/server/recommend.ts:83`
- **Rilevato da**: performance+logic · **Verificato da**: verifier indipendente (batch src/server/recommend.ts)
- **Severità**: INFO
- **Descrizione/evidenza**: `const recLists = await Promise.all(top5.map(({ e }) => fetchRecommendations(e.mediaId).catch(() => [])));` swallows every error (AniListError 502 after 3 retries+429 backoff included) with no log, so the entire community component of the score (WEIGHTS.communityPerHit/communityCap) can silently vanish for a run while the response looks normal — unlike candidates.ts:45 which at least documents the per-query fallback in its comment block.
- **Verifica**: Facts confirmed — recommend.ts:83 `.catch(() => [])` swallows every failure including exhausted-retry AniListErrors with no log anywhere in the file — but no defect is demonstrated: the swallow is the codebase's deliberate degradation posture, identical to candidates.ts:39, and scores degrade gracefully instead of the request breaking. The evidence's contrast is also wrong: candidates.ts's docstring (lines 10-14) documents the query mix, not the per-query fallback, so the two sites are equally u…
- **Fix suggerito**: Log the degradation: `.catch((e) => { console.warn(`community fetch failed for ${e.mediaId}:`, e.message); return []; })` (keep the fallback), so an AniList outage is visible in server logs instead of just lower-quality rankings.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

#### test-quality-9.8 — Il parsing LLM documentato come tolerant (prose/fences) è testato solo sul caso banale: la suite dà falsa sicurezza sul punto più fragile
- **File**: `tests/llm.test.ts:77`
- **Rilevato da**: test-quality · **Verificato da**: verifier indipendente (batch tests/llm.test.ts)
- **Severità**: INFO
- **Descrizione/evidenza**: L'unico input passato a `parseExplanations` è JSON puro generato dal fake: `JSON.stringify(ids.map(...))` (riga 77-78). Il ramo per cui la funzione esiste — commento in src/server/llm.ts:62 `/** Extract the JSON array even when the model wraps it in prose or fences. */`, ovvero `raw.indexOf("[")` / `lastIndexOf("]")` su output con testo attorno o fence ```json, e il path di errore `return []` su JSON malformato (llm.ts:63-75) — non è mai esercitato: un modello che risponde "Sure! ```json [...]```" (casoDominante con modelli locali 8B) non ha alcuna verifica. Segnalato come INFO per la regola a…
- **Verifica**: Fatti verificati: parseExplanations (src/server/llm.ts:63-75) è module-private (export di llm.ts solo llmHealth e explainRecos), unico chiamante llm.ts:129, e l'unico input mai prodotto è JSON.stringify puro dal fake (tests/llm.test.ts:77-78); grep su tutto src+tests non trova altri riferimenti → i rami documentati dal commento llm.ts:62 ('even when the model wraps it in prose or fences': indexOf('[')/lastIndexOf(']') e il path di errore return []) non sono esercitati da nessun test della suite.…
- **Fix suggerito**: Un test tabellare su `parseExplanations` (esportandola o tramite fake handler che restituisce: prose+array, fenced array, array con id non numerico, garbage totale → atteso []) coprirebbe il contratto documentato con ~15 righe.
- **Patch proposta**: nessuna (fix descritto qui sopra nel suggestedFix)
- **Rischio residuo se non corretto**: debito latente

## Falsi positivi scartati

| ID | Titolo | Agente | Motivo scarto |
|---|---|---|---|
| security+logic-2.1 | runLms waits only on 'close' with a one-shot SIGTERM: a hung/orphaned child leav | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.2 | runInstall lacks runLms' `done` latch: on spawn failure Node fires 'error' then  | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.3 | POST /reset deletes config.json but leaves all in-memory state: stale fileConfig | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.4 | ensureLlmServer and startDownload have no mutual exclusion: two concurrent `lms  | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.5 | /status health probe ignores the LLM_BASE_URL env override the rest of the code  | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.6 | downloadedModels caches an empty model list on JSON parse failure, masking a rea | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.7 | detectHardware dereferences spawnSync stdout without checking for spawn error —  | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.8 | installCli on unsupported OS reports success: POST /install-cli returns 200 {ok: | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.9 | /finish custom-backend path silently drops an invalid model instead of rejecting | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.10 | MODEL_KEY_RE whitelist permits path-traversal keys ('..' and '/' are allowed) th | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.11 | URL.pathname used as a filesystem path breaks on Windows (a supported platform h | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+logic-2.12 | RAM threshold compares the rounded-up RAM value with >=, so a 15.x GB machine cr | security+logic | SPECULATIVE: verifier non ha prodotto verdetto |
| security+performance-3.5 | Nonexistent user: data.MediaListCollection is null and .lists is dereferenced un | security+performance | SPECULATIVE: Il deref non guardato esiste (anilist.ts:236 `data.MediaListCollection.lists`, e `list.entries` pure non guardato; gql l |
| security+performance-3.6 | fetchRecommendations crashes on null Media (deleted id), and the caller's catch( | security+performance | SPECULATIVE: Deref non guardato certo nel codice (anilist.ts:316 `data.Media.recommendations`, piu' `n.mediaRecommendation.id` anch'e |
| test-quality-8.3 | Empty-input edges of the pure pipeline untested (first-run user with zero entrie | test-quality | SPECULATIVE: verifier non ha prodotto verdetto |
| test-quality-8.4 | supportMin rejection path exercised by the fixture but never asserted (support=1 | test-quality | SPECULATIVE: verifier non ha prodotto verdetto |
| logic-12.3 | Griglia con minmax(300px,1fr) causa overflow orizzontale a 320px (fallimento WCA | logic | REJECTED: La premessa centrale e' falsa: la media query (styles.css:422-428) NON si somma al minmax, la SOSTITUISCE — stesso selet |
| logic-12.2-dup | (placeholder) | logic | REJECTED: Placeholder senza contenuto: titolo, evidenza e suggestedFix sono letteralmente 'placeholder'. Nessun difetto rivendicat |

## Prossimi passi consigliati

1. **Host/Origin check middleware su tutte le route /api** — security-1.2/1.1, chiude DNS rebinding + abuso da processi locali; sforzo: basso
2. **Ordinare finalizeSides per |aff| prima del cap** (profile.ts) — logic+contract-5.1 / logic-4.1, impatta direttamente la qualità dei consigli su liste grandi; sforzo: basso
3. **Collegare il bottone download del wizard al job reale (POST /download) e non solo a finish()** — logic+security-10.1; sforzo: medio
4. **Rilasciare busy dopo ricerca riuscita in App.tsx** — logic+security-10.2, una riga; sforzo: basso
5. **Escalation SIGKILL in runLms + timeout in runInstall** — logic-2.1/2.3; sforzo: basso
